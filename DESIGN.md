# DESIGN.md — 镜译界面与动效规约

> 状态：常夜灯 2026-09-23 批准，第 12 节按效果图确认。第 11 节「待定项」里的冲突暂不改代码；第 12 节与前文冲突处以第 12 节为准。
> 对应版本：v0.34.0（`index.js` 中 `APP_VERSION`）。
> 第 1–11 节整理的是**现有**取值，每个数值都标注出处；第 12 节是批准后新增的规约。

**出处写法**

| 写法 | 含义 |
|---|---|
| `style.css:29` | 仓库文件与行号（`style.css` / `host.css` / `reading.css` / `index.js` / `core.js` / `palette.js`） |
| `Motion.dc.html:125` | `design/` 下的设计画布与行号；`canvas.json` 为画布总表 |
| 设计稿 v1.2 第 N 页 | `镜译-前端最终设计-v1.2.pdf`（仓库外，常夜灯本机 Downloads），页码为 PDF 页 |
| 实测 | 本地预览宿主 `preview.html` 的计算值：桌面 1184×805；手机模拟 390×844、触屏、`pointer: coarse`、`hover: none` |

---

## 1 适用范围与优先级

### 1.1 覆盖的界面

| 界面 | 所在文档 | 样式文件 | 构建位置 |
|---|---|---|---|
| 悬浮入口（悬浮球 / 胶囊 / 贴边条） | 酒馆主文档 | `host.css:21-287, 418-423` | `index.js:10305-10439` |
| 悬浮窗（小窗 / 卡片 / 满屏） | 独立 shadow root | `style.css:501-1012` 中 `.jy-mini*` | `index.js:10562-12069` |
| 控制中心 | 独立 shadow root | `style.css:1-499` | `index.js:10062-10110`，标记 `index.js:305` 起 |
| 小对话框（要不要分析、保存、重新分析） | 独立 shadow root | `style.css:976-1012` 中 `.jy-ask*` | `index.js:4352-4378`（`ttsDialogShell`）、`4385`、`4434`、`5972` |
| 楼层装饰（段落按钮、每句按钮、楼底栏、朗读高亮） | 酒馆主文档 `#chat` | `host.css:289-541` | `index.js:6262-6308`（`makeTtsBar`）、`6425-6436` |
| 阅读美化（可爱 / 极简 / 原文折叠）与说话人、情绪排版 | 酒馆主文档 | `reading.css`（由 `host.css:1` 引入） | 写入楼层 HTML |

shadow root 内的界面共用一份 `style.css`（`index.js:10575` 等处 `loadPanelCss()`），主题 token 定义在 `:host` 上（`style.css:1-35`）。酒馆主文档里的界面不读主题 token：入口有自己的配色（`host.css:36-44`），楼层装饰全部由周围文字的 `currentColor` 推出（`host.css:289-290` 注释）。

### 1.2 已定的产品规则（不在本文件内讨论数值）

以下规则来自已确认的设计（设计稿 v1.2 第 2、6、7、8 页），任何 UI 改动都不得违背：

1. 入口不改：PC 悬浮球（进度环 + 状态点）、手机贴边条保持现状；收起悬浮窗就是回到入口；入口上不加文字、倒数、播放键（设计稿 v1.2 第 5 页）。
2. 悬浮窗永远是窗：不钉底、不钉顶；满屏态仍是窗，有自己的关闭（`style.css:902` 注释）。
3. 正文上方不新增浮层；楼层内不加播放器、不让播放器跟着句子跳（设计稿 v1.2 第 7 页第 6 条）。
4. 触屏：大文字按钮 46px、播放键 56px 在右下拇指区、图标只用 ▶ ❚❚ ×、可镜像给左手、聊天输入框获得焦点时窗收回贴边条；楼层里触屏不放每句控件。

### 1.3 优先级

1. 本文件批准后，为尺寸、间距、圆角、字号、颜色语义、动效时长与曲线的唯一依据。
2. 本草案的取值以**代码现状**为准（它是已发布的状态）；设计画布、设计稿 v1.2 与代码不一致之处不在此裁定，全部列入第 11 节。
3. 规约变更走「提案 → 常夜灯批准」，不在实现途中临时改。

---

## 2 尺寸与栅格

### 2.1 断点与设备判定

| 判定 | 条件 | 作用 | 出处 |
|---|---|---|---|
| 紧凑视口（脚本） | `(max-width: 767px), (pointer: coarse)` | 悬浮窗 `data-touch`、入口自动变贴边条、音频就绪震动、满屏态可用 | `index.js:10221-10223`；`10246`；`5066`；`10865-10870` |
| 窄桌面 | `max-width: 900px` | 控制中心左栏收窄 | `style.css:342-354` |
| 手机 | `max-width: 640px` | 控制中心单列、顶部页签；输入框字号 16px | `style.css:355-418, 486-499, 602-608, 674-676, 701-715, 921-924`；脚本 `index.js:9170` |
| 手机（悬浮窗补充） | `(max-width: 640px), (hover: none)` | 悬浮窗触控放大（与 `data-touch` 规则重叠，多数被其覆盖） | `style.css:658-665` |
| 小屏对话框 | `max-width: 560px` | 对话框按钮竖排；美化卡片内边距缩小 | `style.css:992-995`；`reading.css:62-65` |
| 调音台单列 | `max-width: 520px` | | `style.css:932-934` |
| 无悬停 | `(hover: none)` | 楼层按钮放大、进度条加粗、极简风按钮常显 | `style.css:742`；`host.css:458-460, 476-490, 526-535`；`reading.css:121-123, 229-231` |

### 2.2 窗口尺寸

**控制中心**

| 项 | 桌面 | ≤640px | 出处 |
|---|---|---|---|
| 对话框 | `min(1180px, 100%)` × `min(820px, 100dvh - 48px)` | `100%` × `min(760px, 88dvh)`，`max-height: calc(100dvh - 24px)` | `style.css:49`；`357` |
| 遮罩内边距 | 24px | `max(12px, safe-area-top) 10px max(12px, safe-area-bottom)` | `style.css:48`；`356` |
| 左栏 | 190px；≤900px 为 152px | 变为顶部横排 | `style.css:56`；`343`；`358-369` |
| 页面内边距 | `22px 26px 0`；≤900px 左右 22px | `25px 18px 0` | `style.css:72`；`348`；`372` |

**悬浮窗**（三种尺寸由 `data-size` 表示）

| 尺寸 | 桌面 | 触屏 | 出处 / 实测 |
|---|---|---|---|
| `compact` 小窗（默认） | 宽 300px | 宽 300px | `style.css:950`；默认值 `index.js:10524`；实测桌面 300×137、手机 300×160 |
| `card` 卡片 | 宽 360px，`max-width: calc(100vw - 20px)`，`max-height: calc(100dvh - 16px)` | 宽 92vw，最宽 400px，`max-height: 70dvh` | `style.css:745, 503, 644`；`867`；实测桌面 360×682（5 段）、手机 359×591 |
| `max` 满屏（仅触屏） | —（桌面自动回到 card） | `100vw` × `100dvh`，圆角 0，无边框 | `style.css:903`；`index.js:10868`；实测 390×844 |
| 键盘弹出 | — | 高度改为 `var(--jy-mini-vh)`（可视视口高） | `style.css:973-974`；判定 `visualViewport.height < innerHeight - 120`，`index.js:11211-11217` |

尺寸切换顺序：`compact → card → (触屏) max → compact`（`index.js:10528-10532`）。选择写入 `localStorage`（`index.js:10534-10540`）。

**悬浮窗定位**

| 项 | 值 | 出处 |
|---|---|---|
| 与视口边距 | 8px | `index.js:10453-10454, 11961-11962` |
| 与入口的间距 | 10px（`MINI_GAP`） | `index.js:193` |
| 挂靠顺序 | 入口上方 → 下方 → 左侧 → 右侧，取第一个不压住入口的位置；右缘对齐入口右缘，放不下改对齐左缘 | `index.js:10446-10473` |
| 拖动后 | 记住位置，下次打开沿用；满屏态不可拖 | `index.js:11954-11965, 11985, 12006` |

**悬浮入口**

| 形态 | 尺寸 | 出处 / 实测 |
|---|---|---|
| 圆环（`ring`） | 42×42，内核 40×40，圆环内缩 3px | `host.css:29-30, 92-93, 150`；实测 42×42 |
| 胶囊（`pill`） | 宽 124px；翻译中 178px | `host.css:54-62` |
| 贴边条（`edge`） | 38×34，贴右缘，只能上下拖 | `host.css:80-86`；`index.js:10356-10359`；实测 38×34 |
| 与视口边距 | 左右上 8px，下 4px | `index.js:10350, 10361` |
| 默认位置 | `x = innerWidth - 76`，`y = innerHeight × 0.62` | `index.js:10302` |

**小对话框**：宽 `min(420px, 100%)`，外壳内边距 16px（`style.css:980-982`）。

### 2.3 悬浮窗分区尺寸

| 分区 | 桌面 | 触屏 | 出处 / 实测 |
|---|---|---|---|
| 标题栏内边距 | `10px 12px` | `12px 12px` | `style.css:519`；`871` |
| 标题栏高 | 实测 44 | 实测 58 | |
| 标题栏按钮 | 24×24 | 34×34 | `style.css:524`；`872` |
| 页签按钮内边距 | `6px 0 8px` | `11px 0 12px` | `style.css:612`；`869` |
| 页签高 | 实测 36 | 实测 47 | |
| 内容区内边距 | `13px 14px 14px`；满屏 `14px 16px` | 同左 | `style.css:526`；`906` |
| 小窗概要内边距 | `10px 12px 12px` | 同左 | `style.css:949` |

### 2.4 触控目标

| 元素 | 桌面 | 触屏 | 出处 / 实测 |
|---|---|---|---|
| 文字按钮（悬浮窗动作区、传输键、段落行按钮） | 32（动作区）/ 36（传输键）/ 30（行按钮） | 46，圆角 12 | `style.css:532, 823, 802`；`876, 878` |
| 小窗两个按钮 | 实测 34 | 44，圆角 12 | `style.css:959` |
| 播放键 | 52 圆 | 56 圆 | `style.css:824`；`880` |
| 标题栏按钮 | 24 | 34 | `style.css:524`；`872` |
| 楼层箭头 ‹ › | 26 | 34 | `style.css:757`；`897` |
| 列表行（段落、句子） | 无下限（实测 58） | 最小 52 | `style.css:778`；`873` |
| 筛选 chip | 实测 23 | 实测 36 | `style.css:634`；`900` |
| 文字链接 | `padding: 8px 4px`（实测 36） | `padding: 6px 4px`，13px（实测 34） | `style.css:98`；`896` |
| 可拖进度条 | 轨 8，旋钮 14 | 轨 10，旋钮 18 | `style.css:735, 737`；`742` |
| 对话框按钮 | 40，圆角 10 | ≤560px 44，整行 | `style.css:989`；`994` |
| 楼层段落按钮（播放 / 重新生成） | `min-height: 1.85em`（实测 23） | 40px | `host.css:509`；`531`；实测手机 66×40 |
| 楼层每句按钮 | 1.3em 圆 | 1.55em 圆 | `host.css:295-296, 430-431`；`480-481` |
| 楼底栏按钮 | `padding: .2em .75em`（实测 27） | `padding: .35em .9em`（实测 31） | `host.css:366`；`487` |
| 控制中心输入框 | 最小 34 | 字号 16px | `style.css:158`；`496` |
| 控制中心手机页签 | — | 最小 54 | `style.css:367` |

---

## 3 间距

代码没有定义间距 token，下表是现状取值。

**高频值**（`style.css` 中 `gap` 出现次数）：8px ×33、6px ×29、10px ×15、4px ×10、12px ×10、9px ×8、5px ×7、3px ×7、7px ×5、16px ×5、20px / 18px ×4、2px ×4。

**按场景**

| 场景 | 值 | 出处 |
|---|---|---|
| 控制中心页面内边距 | `22px 26px 0`（手机 `25px 18px 0`） | `style.css:72, 372`；`Main.dc.html:63` |
| 页头与内容 | 下边距 18px（手机 23px） | `style.css:74, 373` |
| 卡片内边距：主卡 / 摘要卡 / 编辑区 | `20px 22px` / `14px 16px` / `16px 18px` | `style.css:103, 140, 192`；`Main.dc.html:80, 122` |
| 表单分节 | `padding: 18px 0`，标签列 132px（≤900 120px），列距 20px | `style.css:217, 353` |
| 字段之间 | 下边距 19px | `style.css:221` |
| 悬浮窗内容区各块间距 | 12px | `style.css:526` |
| 悬浮窗分节 | 上方 `padding-top: 12px` + 1px `--jy-line-soft` 分隔线 | `style.css:534, 538, 620` |
| 悬浮窗动作区按钮间距 | 6px（触屏 8px） | `style.css:531`；`877` |
| 列表行内边距 | `8px 4px`（触屏 `10px 6px`） | `style.css:778`；`873` |
| 传输键间距 | 10px | `style.css:822` |
| 楼层底栏 | `gap: .35em .8em`，上外边距 .9em，上内边距 .55em | `host.css:350-352` |
| 楼层段落按钮组 | 与正文间距 .45em（触屏 .5em），组内 .3em（触屏 .4em） | `host.css:497-498`；`528` |

---

## 4 圆角

**Token**

| Token | 值 | 用途 | 出处 |
|---|---|---|---|
| `--jy-radius` | 10px | 卡片、日志条、编辑区 | `style.css:27` |
| `--jy-radius-sm` | 6px | 按钮、输入框、菜单、预览行 | `style.css:28` |

**非 token 取值（现状）**

| 值 | 用途 | 出处 |
|---|---|---|
| 50% | 状态点、圆形按钮、播放键、进度旋钮 | `style.css:112, 824, 737` 等 |
| 999px | 药丸、chip、说话人 chip、播放条、入口、楼层按钮 | `style.css:287, 634, 794, 812`；`host.css:37, 368, 512` |
| 20px | 开关轨道 | `style.css:151` |
| 16px | 控制中心对话框（≤640px） | `style.css:357` |
| 14px | 控制中心对话框、小对话框 | `style.css:49, 982` |
| 13px | 悬浮窗 | `style.css:504` |
| 12px | 触屏文字按钮 | `style.css:876, 878, 959` |
| 10px | 小对话框按钮与勾选框、主题菜单；贴边条 `10px 0 0 10px` | `style.css:989, 1004, 478`；`host.css:84` |
| 9px | 连接方式选择卡 | `style.css:212` |
| 8px | 对话框文本框 | `style.css:1003` |
| 7px | 控制中心导航项；悬浮窗结果框、改句原文框、思考框、日志详情 | `style.css:63, 556, 625, 769, 773, 860` |
| 5px | 悬浮窗标题栏按钮；手机导航 `5px 5px 0 0` | `style.css:524, 367` |
| 4px | 徽章、日志正文框；控制中心字标 `4px 11px 4px 4px` | `style.css:101, 326, 59` |
| 3px | 进度轨、色板；悬浮窗字标 `3px 8px 3px 3px` | `style.css:116, 281, 295, 521` |
| 0 | 满屏悬浮窗 | `style.css:903` |
| 单侧圆角 | 列表行 `0 6px 6px 0`、检查框 `0 10px 10px 0`（左侧是 2–3px 竖条） | `style.css:179, 253`；`Rules.dc.html`（10 处 `0 6px 6px 0`） |

---

## 5 字体层级

### 5.1 字族

| 用途 | 字族 | 出处 |
|---|---|---|
| 界面正文 | `"PingFang SC", "Microsoft YaHei UI", "Microsoft YaHei", system-ui, -apple-system, "Segoe UI", sans-serif` | `style.css:33`；`Baseline.dc.html:111` |
| 「镜」字徽标 | `Georgia, "Songti SC", serif` | `style.css:59, 521` |
| 等宽（请求、返回、音色 ID） | `ui-monospace, "Cascadia Mono", Consolas, monospace` 及 `Consolas, ui-monospace, monospace` 等写法 | `style.css:253, 273, 309, 321, 326, 631, 860` |
| 入口文字 | `system-ui, "Microsoft YaHei", sans-serif` | `host.css:222, 228` |
| 楼层装饰 | 继承楼层（`font: inherit`） | `host.css:305, 372` |

基准：`13px / 1.7`（`style.css:33`）。

### 5.2 层级（控制中心与悬浮窗）

| 层级 | 字号 / 字重 | 出处 |
|---|---|---|
| 运行状态大字 | 22 / 650，字距 -.01em | `style.css:110`；`Main.dc.html:84` |
| 页面标题 h1 | 17 / 650，字距 -.01em（≤640px 为 24） | `style.css:44`；`374`；`Baseline.dc.html:96-97` |
| 品牌字 | 控制中心 17 / 700，字距 .16em；悬浮窗 13 / 650，字距 .1em | `style.css:60`；`522, 753` |
| 卡片标题 h2 | 14 / 650 | `style.css:45`；`Baseline.dc.html:100-101` |
| 悬浮窗状态标题 | 14 / 650 | `style.css:529, 756, 819` |
| 正文与表单值 | 13 / 400 | `style.css:33`；`Baseline.dc.html:104-105` |
| 悬浮窗句子、改句原文 | 12.5 | `style.css:625, 790` |
| 按钮 | 12 / 600（控制中心大按钮 13） | `style.css:94`；`138` |
| 次要说明（`.jy-muted`） | 12，行高 1.7 | `style.css:78` |
| 悬浮窗译文、思考行 | 11.5 | `style.css:769, 789` |
| 字段标签、状态、overline | 11 / 600–700 | `style.css:77, 107, 156`；`Baseline.dc.html:108-109` |
| 药丸 | 10.5 | `style.css:794` |
| 徽章 | 10 | `style.css:101` |
| 最小 | 9（字标副题、手机 overline、页签出错数） | `style.css:61, 375, 751` |

触屏悬浮窗：段落译文与句子 14（`style.css:875`），按钮 14（`876, 878`），页签 13（`869`），药丸 12（`895`），文字链接 13（`896`），日志正文 13（`899`），chip 13（`900`），文本框 14（`901`）。

字重实际用到 400 / 500 / 600 / 650 / 700，650 最多（`style.css` 中 25 处）。

行高：正文 1.7（`style.css:33`）；文本框 1.8（`162`）；悬浮窗结果与阅读美化 1.85（`557`；`reading.css:6`）；日志思考 1.9（`330`）；辅助信息 1.5–1.6。

### 5.3 楼层内

楼层装饰用 em，随楼层字号缩放：底栏 .84em（`host.css:354`），段落按钮 .82em（触屏 .9em，`host.css:513, 533`），每句按钮图标 .62em（`host.css:316-317`）。

说话人与情绪排版（`reading.css:185-217`）：12 种情绪各有字重、斜体、字距、字号的两级变化，例如愤怒 640 / 760、呼喊 700 1.06em / 820 1.12em、低语 340 斜体 .95em / 280 斜体 .9em。颜色由 `palette.js` 计算（见 6.5）。

---

## 6 颜色语义

### 6.1 主题

共五套，由宿主元素的 `data-theme` 切换；默认 `day`（`core.js:597, 1171`）。界面名称：日间、夜间、护眼小清新、华美吸血鬼、极简毛玻璃（`index.js:312`）。

### 6.2 Token 与用途

| Token | 用途 | day `style.css:3-26` | night `:422-431` | fresh `:432-440` | vampire `:441-450` | glass `:455-465` |
|---|---|---|---|---|---|---|
| `--jy-paper` | 控制中心底、吸底页脚 | #f6f4ee | #191e25 | #edf3e7 | #20131b | rgba(243,246,248,.83) |
| `--jy-surface` | 卡片、按钮、悬浮窗底 | #fffdf7 | #222932 | #f7faee | #2b1a23 | rgba(255,255,255,.55) |
| `--jy-field` | 输入框底 | #fffef9 | #222932 | #f7faee | #2b1a23 | rgba(255,255,255,.55) |
| `--jy-text` | 正文 | #22332e | #e4e7ed | #344932 | #f0ddd6 | #293b46 |
| `--jy-muted` | 次要文字 | #6b7770 | #a6b0bf | #6b7c58 | #b99c9d | #637b85 |
| `--jy-muted-soft` | 更弱的说明、空闲状态点 | #8b958c | #8994a3 | #8a9a78 | #9c8285 | #7d939c |
| `--jy-placeholder` | 占位符 | #919b8b | #7d8797 | #8fa07d | #8d7376 | #8aa0a9 |
| `--jy-line` | 默认边框 | #d8dccf | #39434d | #cfdbc0 | #50313e | rgba(102,131,151,.25) |
| `--jy-line-soft` | 分隔线、列表行上边线 | #ebeade | #2e3741 | #dbe5cd | #3d242f | rgba(102,131,151,.18) |
| `--jy-line-strong` | 悬浮窗外框、悬停边框 | #c2c9ba | #4a5663 | #b7c8a5 | #6b4152 | rgba(102,131,151,.42) |
| `--jy-accent` | 主按钮、选中、聚焦边框、进度填充 | #255d4e | #a4c6bb | #527548 | #d5b174 | #43697b |
| `--jy-on-accent` | 主色上的文字 | #fffef3 | #172b25 | （继承 day） | #28171c | （继承 day） |
| `--jy-accent-soft` | 悬停底、进度轨、当前句 | #e7ede2 | #303e3c | #dde9d0 | #442631 | rgba(216,231,239,.7) |
| `--jy-accent-tint` | 已改动 / 已选中浅底 | #eaefe3 | #2a3a37 | #dcead0 | #3b2330 | rgba(214,232,240,.72) |
| `--jy-accent-ink` | 强调文字（已改动的状态字、药丸字） | #4a6357 | #b7d5c9 | #43613b | #e2c08a | #35586a |
| `--jy-band` | 次级区块底（思考、结果、旁白行） | #eef1e7 | #232b34 | #e3ecd8 | #2e1c26 | rgba(226,238,244,.6) |
| `--jy-band-line` | 次级区块边框 | #dfe3d5 | #3a4550 | #c6d5b6 | #573544 | rgba(102,131,151,.3) |
| `--jy-rail` | 控制中心左栏、悬浮窗标题栏与页签底 | #163a30 | #11191c | #354d32 | #190b13 | rgba(38,63,79,.86) |
| `--jy-switch-off` | 开关关闭轨道 | #bcc6b8 | #49535f | #bcc9ae | #5b3846 | rgba(102,131,151,.4) |
| `--jy-focus` | 焦点环；也用于「进行中 / 需注意」金色 | #b18537 | #d7b26a | #a8842f | #d5b174 | #b1863c |
| `--jy-ok` | 完成 | #3f7a5f | #7fc4a4 | #4d8a5c | #8fc0a0 | #3f8474 |
| `--jy-warn` | 进行中状态点、警告日志 | #b08a3c | #d8b26a | #a8842f | #d5b174 | #b1863c |
| `--jy-error` | 出错、危险 | #b54332 | #e88b7c | #b04a37 | #e8908d | #b54332 |
| `--jy-error-soft` | 出错边框与浅底 | #e2c3bc | #6b3b36 | #d9b3ab | #7a3f43 | rgba(181,67,50,.35) |

`--jy-focus` 的非焦点用法：未保存提示（`style.css:208`）、隐私提示（`334`）、进行中批次（`763`）、估时（`768`）、进行中段落标记（`785`）。

**主题专属覆盖**

| 主题 | 覆盖 | 出处 |
|---|---|---|
| vampire | 页标题衬线体、字距 .06em、字重 500；主卡描边 #856340 与硬阴影；字标描边；导航选中色 | `style.css:451-454` |
| glass | 控制中心 `backdrop-filter: blur(24px) saturate(1.15)`；遮罩 #26374555；页脚模糊 14px | `style.css:466-471` |
| glass · 悬浮窗 | field rgba(255,255,255,.9)、surface rgba(246,249,251,.94)、accent-soft rgba(216,231,239,.9)、外框 rgba(93,121,138,.45)、`blur(22px) saturate(1.08)` | `style.css:508-516` |

### 6.3 深色底上的固定配色（左栏、悬浮窗标题栏与页签）

这组颜色是字面值，不随主题变：

| 值 | 用途 | 出处 |
|---|---|---|
| #cfe0d5 | 左栏文字 | `style.css:57` |
| #a9c0b2 | 导航项、标题栏按钮、未选中页签 | `style.css:63, 524, 612` |
| #f2f7f0 | 悬停 / 选中文字、品牌字 | `style.css:60, 66, 525, 613-614` |
| #e7eee3 | 悬浮窗标题栏文字 | `style.css:519` |
| #e2cf9c | 「镜」字、选中页签下划线 | `style.css:59, 521, 750` |
| #91ad9680 | 字标描边 | `style.css:59, 521` |
| #e6eadb / #204737 | 控制中心导航选中底 / 字 | `style.css:67` |

### 6.4 状态语义

| 状态 | 颜色 | 符号与写法 | 出处 |
|---|---|---|---|
| 完成 | `--jy-ok` | ✓ + 名词（已译 · 细读 3/3） | `style.css:115, 724, 762, 783`；设计稿 v1.2 第 12 页 |
| 进行中 | `--jy-focus`（批次、段落、估时）；`--jy-warn`（状态点） | ◌ + 进度 + 秒 | `style.css:763, 768, 785`；`113` |
| 待做 | `--jy-muted` | 灰字，不加符号 | `style.css:728, 792`；设计稿 v1.2 第 12 页 |
| 出错 | `--jy-error`，浅底 `--jy-error-soft` | ! + 人话 + 一个动作 | `style.css:114, 269-270, 784, 797` |
| 已改动 | 左竖条 `--jy-accent` + `--jy-accent-tint` 底 + `--jy-accent-ink` 字 | | `style.css:181-183`；`Baseline.dc.html:162-166` |
| 选中 | `--jy-accent` 实底 + `--jy-on-accent` 字 | | `style.css:184`；`Baseline.dc.html:156-160` |

### 6.5 酒馆主文档里的颜色

**入口**（`host.css`，不读主题 token，按主题另写一套）

| 项 | day | 出处 |
|---|---|---|
| 底 / 描边 / 图标 | #173b31 / #4d7a68 / #e7d6a6 | `host.css:36-39` |
| 状态色：空闲 / 翻译中 / 完成 / 出错 | #96a1ad / #f0c56e / #8ee1d6 / #f18f8f | `host.css:44, 252-254`；`MiniStates.dc.html:116-119` |
| 进度环轨道 | #35604f | `host.css:157` |
| 胶囊文字 / 计数 | #e7eee3 / #9db4a6 | `host.css:221, 227` |
| 焦点描边 | #b9f3eb | `host.css:274` |
| night / fresh / vampire / glass | 底、字、描边各一套 | `host.css:418-423` |

**楼层装饰**（全部由 `currentColor` 混合）

| 项 | 值 | 出处 |
|---|---|---|
| 每句按钮 | 描边 30%、底 7%；悬停 16%；播放中 24%；静置不透明度 .5 | `host.css:300-307, 324-325, 342` |
| 编辑按钮 | 描边 22%，静置不透明度 .32（触屏 .5） | `host.css:435, 442, 459` |
| 底栏 | 上边虚线 22%；按钮描边 32%、底 8%，悬停 18% | `host.css:353, 367-369, 378` |
| 焦点描边 | 60% | `host.css:331, 455` |
| 出错文字 | #e0786b | `host.css:391-392` |
| 朗读高亮 | rgba(240,197,110,.36) | `host.css:401-403` |
| 勾选框 | `accent-color: var(--SmartThemeQuoteColor, #2f6b4f)`（酒馆变量） | `host.css:538` |

**阅读美化**：可爱风 #bf8792 / #d597a3 / #c88b95（`reading.css:20-35, 75-106`），极简风 #879b92（`reading.css:40, 110-150`），原文折叠 #91a196 / #879b92（`reading.css:48-61, 154-183`）。三种风格分别给播放按钮、编辑按钮、底栏和高亮换色（`reading.css:67-183, 224-237`）。

**说话人配色**（`palette.js`）：颜色由程序计算，不由副模型给出；正文对比度下限 4.5（`palette.js:20`），说话人之间色相至少相距 24°（`palette.js:22`），情绪色相偏移不超过 8°（`palette.js:26`）。

### 6.6 遮罩

| 场景 | 值 | 出处 |
|---|---|---|
| 控制中心 | #071b18b3（glass 主题 #26374555） | `style.css:48, 467` |
| 小对话框（顶层 `::backdrop`） | #0007 | `style.css:981` |
| 小对话框（不支持 `showModal` 时的回退） | #0006 | `style.css:977` |

---

## 7 阴影与层级

### 7.1 阴影

| 用途 | 值 | 出处 |
|---|---|---|
| 控制中心 | `0 26px 70px #07181359` | `style.css:49` |
| 悬浮窗 | `0 20px 52px rgba(4,14,11,.55)` | `style.css:505`；`Mini.dc.html:27` |
| 小对话框 | `0 18px 48px #0004` | `style.css:982` |
| 主题菜单 | `0 12px 36px #0004` | `style.css:478` |
| 下拉菜单 | `0 8px 24px #0000001f`；方案菜单 `0 6px 18px #163a3012` | `style.css:170, 88` |
| 播放键 | `0 6px 16px rgba(4,14,11,.22)` | `style.css:824` |
| 输入聚焦光晕 | `0 0 0 3px` accent 16% | `style.css:161` |
| 进度旋钮描边 | `0 0 0 2px var(--jy-surface)` | `style.css:737` |
| 入口 | `0 7px 20px #07181344`；悬停 `0 13px 36px rgba(0,0,0,.5), 0 0 0 4px rgba(117,213,201,.15)`；贴边条 `-4px 6px 18px #07181366` | `host.css:40, 261, 85` |
| vampire 主卡 | `5px 5px 0 #140b11` | `style.css:452` |

### 7.2 z-index

**酒馆主文档**

| 层 | 值 | 出处 |
|---|---|---|
| 悬浮入口 | 2147482900 | `host.css:23` |
| 悬浮窗宿主 | 2147482950 | `index.js:10572` |
| 控制中心宿主 | 2147483000 | `index.js:10074` |
| 小对话框宿主（询问 / 保存 / 重新分析） | 2147483000；支持 `showModal` 时进入浏览器顶层 | `index.js:4390, 4439, 5977`；`4352-4378` |
| 小对话框回退遮罩 | 2147483000 | `style.css:977` |

**shadow root 内**

| 层 | 值 | 出处 |
|---|---|---|
| 吸底页脚 | 4 | `style.css:206` |
| 方案菜单 | 5 | `style.css:88` |
| 下拉菜单 | 10 | `style.css:170` |
| 关闭按钮 | 30 | `style.css:54` |
| 外观选择（≤640px） | 35 | `style.css:487` |
| 外观菜单 | 40 | `style.css:478` |

---

## 8 动效

### 8.1 Token

| Token | 值 | 用途 | 出处 |
|---|---|---|---|
| `--jy-enter` | `200ms cubic-bezier(.2, .8, .24, 1)` | 进入 | `style.css:29`；`Motion.dc.html:125-126, 208` |
| `--jy-exit` | `130ms cubic-bezier(.4, 0, 1, 1)` | 退出 | `style.css:30`；`Motion.dc.html:134-135, 209` |
| `--jy-hover` | `120ms ease` | 悬停、颜色、折叠箭头 | `style.css:31`；`Motion.dc.html:188-189` |

### 8.2 曲线（现状）

| 曲线 | 用在 | 出处 |
|---|---|---|
| `cubic-bezier(.2, .8, .24, 1)` | 悬浮窗打开、控制中心打开、悬浮窗切页 | `style.css:29, 49, 653` |
| `cubic-bezier(.4, 0, 1, 1)` | 悬浮窗关闭 | `style.css:30` |
| `cubic-bezier(0.22, 0.72, 0.2, 1)` | 抽屉展开、切页高度、入口形态变化 | `style.css:259, 548, 648`；`host.css:46-48` |
| `cubic-bezier(0.3, 0.7, 0.4, 1)` | 入口进度环 | `host.css:168` |
| `linear` | 进度条、遮罩、旋转 | `style.css:48, 136, 726, 736`；`host.css:141, 247` |
| `ease` / `ease-out` / `ease-in-out` | 悬停、淡入、控制中心切页、呼吸 | `style.css:31, 73, 130` |

### 8.3 场景

| 场景 | 时长 | 曲线 | 变化 | 出处 |
|---|---|---|---|---|
| 悬浮窗打开 | 200ms | 进入 | opacity 0→1，scale .82→1，原点右下 `100% 100%` | `style.css:506, 517` |
| 悬浮窗关闭 | 130ms | 退出 | opacity →0，scale →.82；宿主 160ms 后移除 | `style.css:518`；`index.js:12067` |
| 满屏态 | 无进场动画 | — | | `style.css:903` |
| 控制中心打开 | 220ms | `(.2,.8,.24,1)` | opacity，scale .97→1，translateY 10→0 | `style.css:49-50`；`Motion.dc.html:143-144` |
| 控制中心遮罩 | 120ms | linear | opacity | `style.css:48, 51` |
| 控制中心关闭 | 立即移除 | — | | `index.js:10100-10110` |
| 控制中心切页 | 140ms | ease-out | opacity，translateY 4→0 | `style.css:52, 73` |
| 悬浮窗切页 | 240ms | `(0.2,0.8,0.24,1)` | opacity，translateX ±16px（按页签顺序决定方向） | `style.css:653-656`；`index.js:10796, 10815-10818` |
| 悬浮窗切页时的高度 | 260ms | `(0.22,0.72,0.2,1)` | height；420ms 兜底收尾 | `style.css:648`；`index.js:10802-10814` |
| 随手翻抽屉 | 行高 320ms；内容淡入 180ms ease，延迟 140ms | `(0.22,0.72,0.2,1)` | `grid-template-rows: 0fr → 1fr` | `style.css:548-552` |
| 导出抽屉 | 行高 260ms；内容淡入 180ms，延迟 120ms | `(0.22,0.72,0.2,1)` | 同上 | `style.css:259-262` |
| 入口形态（环 ↔ 胶囊） | 380ms | `(0.22,0.72,0.2,1)` | width、border-radius、padding | `host.css:45-48` |
| 入口胶囊文字 | 200ms ease，延迟 210ms | | opacity | `host.css:203, 210` |
| 入口悬停 | 160ms ease | | 不透明度 .86→1，translateY -1px（贴边条 translateX -2px），阴影 | `host.css:42, 49-51, 256-268` |
| 入口胶囊停留 | 2200ms（关窗后 1800ms） | | 完成 / 出错后停留再缩回环 | `index.js:10213, 10272-10292, 12066` |
| 进度环 | 400ms | `(0.3,0.7,0.4,1)` | stroke-dashoffset；换色 260ms ease | `host.css:168` |
| 进度条 | 240ms | linear | scaleX | `style.css:136`；`host.css:141, 247` |
| 可拖进度条 | 120ms | linear | width；旋钮淡入 120ms ease | `style.css:736-737` |
| 悬停 | 120ms | ease | 背景、边框、颜色 | `style.css:31` 及各处 |
| 开关 | .15s | ease（默认） | 旋钮 translateX 17px | `style.css:152, 154` |
| 折叠箭头 | 120ms | ease | rotate -45°→45° | `style.css:681-682, 690-691`；`126-127` |
| 播放键悬停 | 120ms | ease | scale 1.04 | `style.css:824-825` |
| 楼层播放中 | 120ms | ease | 每句按钮 scale 1.08（段落按钮不缩放） | `host.css:311, 343, 521-524` |
| 说话人颜色切换 | 120ms | ease | color | `reading.css:193` |

**循环动画**

| 动画 | 周期 | 出处 |
|---|---|---|
| 思考中呼吸点 | 1.6s ease-in-out，opacity .3↔1，scale .8↔1.15 | `style.css:130-131` |
| 批次、进行中段落 | 1.2s ease-in-out，opacity 1↔.45 | `style.css:763-764, 785` |
| 步骤进行中圆圈 | .9s linear 旋转 | `style.css:726, 731` |
| 楼层按钮生成中 | 1.1s ease-in-out，opacity .45↔1 | `host.css:335-338, 405-408` |

**按压**：没有独立的 `:active` 视觉，只切换拖动光标（`style.css:740`；`host.css:270-271`）。

### 8.4 减少动态效果（`prefers-reduced-motion: reduce`）

| 范围 | 处理 | 出处 |
|---|---|---|
| shadow root 全部 | `transition: none !important; animation: none !important; scroll-behavior: auto !important` | `style.css:419` |
| shadow root 局部 | 时长改 1ms 或关闭循环（与上条并存） | `style.css:336-340, 562-568, 907-909` |
| 悬浮窗切页 | 脚本检测后不做高度与滑入动画 | `index.js:10801-10802` |
| 入口 | 过渡改 1ms、延迟 0 | `host.css:278-287` |
| 楼层按钮 | 关闭动画与过渡 | `host.css:410-416` |
| 说话人颜色 | 关闭过渡 | `reading.css:220-222` |

---

## 9 组件规约

### 9.1 按钮

| 类型 | 类名 | 规格 | 出处 |
|---|---|---|---|
| 次要（默认） | `.jy-button` | 最小高 32，`padding: 6px 13px`，1px `--jy-line`，圆角 6，底 `--jy-surface`，12 / 600，图标间距 6；悬停底 `--jy-accent-soft`、边框 `--jy-line-strong` | `style.css:94-95`；`Baseline.dc.html:125` |
| 主要 | `.jy-button-primary` | 底与边框 `--jy-accent`，字 `--jy-on-accent`；悬停 `brightness(.94)` | `style.css:96-97`；`Baseline.dc.html:124` |
| 文字 | `.jy-text-button` | 无边框，字 `--jy-accent`，`padding: 8px 4px`，12；悬停下划线 | `style.css:98-99`；`Baseline.dc.html:126` |
| 图标 | `.jy-icon-button` | 30×30 起，1px 边框，圆角 6，字 `--jy-accent`，16 | `style.css:100` |
| 危险 | 行内：`.jy-mini-row-button-danger`；动作区「停止」 | 边框与字 `--jy-error` | `style.css:803, 533`；`Baseline.dc.html:127` |
| 禁用 | `button:disabled` | 不透明度 .48，光标 wait | `style.css:40` |
| 焦点 | `:focus-visible` | 2px `--jy-focus` 描边，外移 3px | `style.css:42` |
| 控制中心大按钮 | `.jy-launch .jy-button` | 最小高 38，`padding: 0 20px`，13 | `style.css:138`；`Main.dc.html:112` |
| 对话框按钮 | `.jy-ask-actions button` | 最小高 40（≤560px 44 并整行），圆角 10，13.5；主键 `.is-primary` 底 `--jy-accent`、字 #fff | `style.css:989-996` |

### 9.2 药丸、chip、徽章

| 类型 | 类名 | 规格 | 出处 |
|---|---|---|---|
| 状态药丸 | `.jy-mini-pill` | 圆角 999，`padding: 1px 7px`，10.5，底 `--jy-accent-soft`、字 `--jy-accent-ink`；`data-state` 为 done / active / error / plain / pending 各有配色；触屏 12，`padding: 2px 9px` | `style.css:794-799`；`895` |
| 筛选 chip | `.jy-mini-chip` | 圆角 999，1px `--jy-line`，11，`padding: 3px 9px`；按下态 `aria-pressed="true"` 实底 `--jy-accent`；触屏 13，`padding: 8px 12px` | `style.css:634-635, 841`；`900` |
| 说话人 chip | `.jy-speaker-chip` | 圆角 999，底 `--jy-band`，11.5；未上色的名字虚线描边 | `style.css:287, 290` |
| 对话框快捷说法 | `.jy-ask-chips button` | 圆角 999，12，`padding: 4px 10px` | `style.css:1009-1010` |
| 徽章 | `.jy-badge` | 圆角 4，1px `--jy-line`，底 `--jy-accent-soft`，字 `--jy-muted`，10 | `style.css:101` |
| 页签出错数 | `[data-errors]::after` | 高 14，圆角 7，底 `--jy-error`，字 #fff，9；上限 99 | `style.css:751`；`index.js:11940` |

### 9.3 开关与勾选

| 类型 | 规格 | 出处 |
|---|---|---|
| 开关 `.jy-switch` | 轨道 42×25，圆角 20；旋钮 17×17 白色，内缩 4px，开启右移 17px，.15s；关 `--jy-switch-off`，开 `--jy-accent`；焦点描边同按钮 | `style.css:149-155` |
| 勾选框 | 16×16，`accent-color: var(--jy-accent)` | `style.css:148` |
| 勾选行 `.jy-check` | 间距 8，`padding: 7px 0`，12 | `style.css:147` |
| 二选一卡片 `.jy-connection-choice` | 两列，间距 12；卡片 `padding: 13px 16px`，圆角 9；选中边框 `--jy-accent`、底 `--jy-accent-tint` | `style.css:209-216`；`Connection.dc.html:82, 89` |

### 9.4 输入框

最小高 34，`padding: 7px 11px`，1px `--jy-line`，圆角 6，底 `--jy-field`；聚焦边框 `--jy-accent` + 3px 光晕；占位符 `--jy-placeholder`；≤640px 字号 16（`style.css:157-164, 496`；`Baseline.dc.html:131-132`）。标签 `.jy-label` 11 / 650，`--jy-muted`，下边距 7（`style.css:156`）。

### 9.5 折叠

| 形式 | 标识 | 动效 | 出处 |
|---|---|---|---|
| 分节折叠 `.jy-fold` | 7px 描边箭头，2px `--jy-accent` | 旋转 120ms | `style.css:678-686` |
| 角色行 `.jy-tts-voice-row` | 6px 描边箭头，1.5px `--jy-muted` | 旋转 120ms | `style.css:687-691` |
| 调音台与标点标签 | 文字 ▸ / ▾ | 无 | `style.css:927-931, 937-941` |
| 随手翻 | 文字 ＋ / − | 行高 320ms | `style.css:542-552` |
| 思考框 | 实心三角 | 旋转 180°，120ms | `style.css:126-127` |
| 抽屉原理 | `grid-template-rows: 0fr → 1fr`，内容 `overflow: hidden; min-height: 0`，内容淡入晚于行高 | | `style.css:258-262, 546-552` |

### 9.6 对话框

**控制中心**：见 2.2；关闭按钮 32×32 圆形，底 `--jy-accent-soft`（`style.css:54, 472-473`）；Esc 关闭，Tab 在对话框内循环（`index.js:10114-10133`）；点遮罩关闭（`index.js:10111-10113`）。

**小对话框**（`.jy-ask`）：宽 `min(420px, 100%)`，圆角 14，`padding: 16px 16px 14px`，13.5 / 1.6；标题 15（`style.css:982-983`）。外壳优先 `<dialog>` + `showModal()`，否则回退为 fixed 遮罩；点暗处关闭，Esc（`cancel`）关闭（`index.js:4352-4378`）。

### 9.7 悬浮窗

**结构**（`index.js:10586-10716`，自上而下）：标题栏 → 小窗概要 → 页签 → 三页（翻译 / 朗读 / 日志）。

| 部件 | 规格 | 出处 |
|---|---|---|
| 外框 | 圆角 13，1px `--jy-line-strong`，底 `--jy-surface`，纵向 flex，内容区滚动 | `style.css:502-506, 644-646` |
| 标题栏 | 底 `--jy-rail`，整条可拖（按钮除外），`touch-action: none`；字标 22×24；标题「第 N 楼 · 状态」+ 6px 状态点；右侧三个按钮：尺寸切换（⤢ / ⤡）、控制中心（⚙）、收起（−） | `style.css:519-525`；`index.js:10587-10593, 10846-10851, 11985` |
| 小窗概要（`compact`） | 楼层行 13 / 650，任务行 12 `--jy-muted`，播放条，两个等宽按钮「读这一楼」「展开」（主要） | `style.css:949-959`；`index.js:10594-10603` |
| 页签 | 三等分，12 / 650，字距 .06em；选中字 #f2f7f0 + 2px #e2cf9c 下划线；朗读关闭时隐藏「朗读」 | `style.css:611-614, 746-750`；`index.js:10604-10608, 10828-10832` |
| 翻译页 | 状态区（‹ 楼层 ›、状态、批次条 6px、任务行 + 金色估时、思考行）→ 段落列表 → 动作区 → 快捷行（翻译模型、方案、未译楼入口）→ 随手翻 → 播放条 | `index.js:10609-10662`；`style.css:755-817` |
| 动作区 | 自适应网格，列最小 64px；「翻译本楼」与「停止」为主要按钮，按状态显隐；「自动 开 / 关」为开关按钮 | `style.css:805-807`；`index.js:10625-10631` |
| 段落行 | 三列 `16px / 1fr / auto`，行间 1px `--jy-line-soft`，圆角 6；原文 11 灰单行，译文 11.5；编辑态底 `--jy-accent-soft` | `style.css:778-803` |
| 播放条 | 圆角 999，1px `--jy-line`，进度 4px；只在朗读进行中出现，点击切到朗读页 | `style.css:812-817`；`index.js:10656-10661` |
| 朗读页 · 播放器 | 标题 14 / 650；可拖进度条；两端时间 11；传输键「上一段 / ▶ / 下一段」；药丸；步骤清单（最高 190）；文字链接 | `style.css:818-830, 716-742`；`index.js:10664-10682` |
| 朗读页 · 句子列表 | 每行：标记、句子（两行截断）、标签、文字按钮「播放 / 重新生成 / 缓存 / 详细」；当前句底 `--jy-accent-soft` | `style.css:778-790, 961-966`；`index.js:11474-11488` |
| 朗读页 · 改句面板 | 说话人选择、原文、发给 Fish 的文本（等宽 12）、标签 chip、自定义指令、语速与音量、「重新生成并播放」（改过才出现） | `style.css:620-642, 969-972`；`index.js:10685-10702` |
| 日志页 | 筛选 chip「本楼 / 全部 / 翻译 / 朗读 / 只看出错」+「复制全部」；每条两行（元信息 10.5 + 正文 12 两行截断）；出错条浅红底；详情页 | `style.css:839-862`；`index.js:10704-10716` |
| 桌面快捷键 | Esc 收起；窗体聚焦且未在输入时：Space 播放 / 暂停，← → 进度 5%，[ ] 上一段 / 下一段；触屏不响应 | `index.js:12080-12100` |

**左手模式**：动作区与传输键整行镜像（`style.css:864-865`；设置 `core.js:612`，`index.js:397`）。

### 9.8 悬浮入口

| 项 | 规格 | 出处 |
|---|---|---|
| 形态选择 | 设置「悬浮入口形态」：自动 / 始终圆环 / 始终胶囊 / 始终贴边；自动时紧凑视口为贴边条，翻译中或停留期为胶囊，否则为圆环 | `index.js:397, 10241-10249`；`core.js:606` |
| 圆环 | 42 圆，边框 1px，不透明度 .86；进度环 viewBox 36、r 16，轨道 2、进度 2.5；图标 16 | `host.css:21-52, 148-187` |
| 胶囊 | 124 宽（翻译中 178），显示状态文字 11 / 600 与百分比 10；翻译中显示 3px 进度条 | `host.css:54-62, 194-250` |
| 贴边条 | 38×34，圆角 `10px 0 0 10px`，无右边框；6px 状态点 + 14px 图标；翻译中底部 2px 进度线 | `host.css:80-146` |
| 状态文字 | 点击打开 / 翻译中 / 已完成 / 未完成 | `index.js:10214-10219` |
| 交互 | 单击开关悬浮窗；拖动超过 6px 视为拖动，其后 300ms 内的点击被吞掉；Enter / Space 打开；位置存本机 | `index.js:10373-10411` |

### 9.9 楼层按钮

| 部件 | 规格 | 出处 |
|---|---|---|
| 设置「楼层里的朗读按钮」 | `line` 每段一对（默认，手机也有）/ `sentence` 每段一对再加每句一对 / `off` 不显示 | `core.js:610, 635`；`index.js:397, 10225-10239` |
| 段落按钮 | 每段末尾「播放」「重新生成」两个胶囊，图标 + 文字，.82em，最小高 1.85em（触屏 40px）；播放中不缩放 | `host.css:492-535`；`index.js:6204-6214, 6218-6235` |
| 每句按钮 | 每句后播放（1.3em 圆）+ 编辑（1.3em 圆，更淡）；触屏 1.55em | `host.css:291-344, 425-460, 476-484`；`index.js:6430-6434` |
| 楼底栏 | 每楼都有：「朗读本楼」（双语时「朗读译文」「朗读原文」）、说明文字（不透明度 .6）、状态（`aria-live`）、「暂停」「停止」（播放时出现）、「缓存」及选择模式（全选 / 缓存所选 / 退出选择 / 计数）、「朗读面板」文字链接 | `index.js:6262-6308, 6436`；`host.css:346-399, 461-475, 537-541` |
| 朗读高亮 | `::highlight(jy-tts-current)`；美化风格各有高亮色，极简风改为下划线 | `host.css:401-403`；`reading.css:88-91, 128-133, 165-168` |
| 颜色 | 取周围文字颜色，不读主题；美化风格内取卡片主色 | `host.css:289-290`；`reading.css:67-72` |

---

## 10 移动端规则

| 规则 | 实现 | 出处 |
|---|---|---|
| 设备判定 | 脚本：宽 ≤767px 或粗指针为紧凑视口；样式：≤640px 或 `hover: none` | `index.js:10221-10223`；`style.css:355, 658` |
| 入口 | 自动变贴边条，贴右缘，只上下移动 | `index.js:10246, 10356-10359`；`host.css:80-146` |
| 卡片尺寸 | 宽 92vw，最宽 400，最高 70dvh | `style.css:867` |
| 满屏态 | 仅触屏；仍是窗，有标题栏按钮；顶部与底部留安全区 | `style.css:902-906`；`index.js:10528-10532, 10868` |
| 页签与主按钮在底部 | 标题栏 `order: 1`、内容 `order: 2`、页签 `order: 3` | `style.css:868-871` |
| 内容不整页滚动 | 内容区 `overflow: hidden`，中间的列表（段落、句子、改句、日志）各自滚动，底部按钮不动 | `style.css:891-894` |
| 大按钮 | 文字按钮 46（小窗 44），圆角 12，14；播放键 56 | `style.css:876-880, 959` |
| 朗读页顺序 | 列表在上、播放器在下；播放器内：链接 → 药丸 / 步骤 → 标题 → 进度条 → 时间 → 传输键 | `style.css:881-890` |
| 进度条 | 轨 10，旋钮 18 常显 | `style.css:742` |
| 列表行 | 最小 52；原文单行、译文与句子最多 4 行，14 | `style.css:873-875` |
| 句子行按钮 | 换到第二行，左对齐，13，`padding: 6px 2px` | `style.css:964-966` |
| 左手模式 | 动作区与传输键镜像 | `style.css:864-865` |
| 键盘避让 | 窗内输入框获得焦点：窗缩到可视视口高度，260ms 后把输入框滚到中间 | `index.js:11209-11222`；`style.css:973-974` |
| 聊天输入框获得焦点 | 悬浮窗关闭，回到贴边条 | `index.js:12101-12106` |
| 桌面快捷键 | 触屏不响应（Esc 除外） | `index.js:12088` |
| 音频就绪 | 震动 30ms | `index.js:5064-5066` |
| 楼层 | 默认每段一对按钮（触屏 40px 高），每句按钮需手动开启；无悬停时按钮常显 | `core.js:610`；`host.css:458-460, 476-490, 526-535`；`reading.css:121-123, 229-231` |
| 控制中心 | ≤640px：顶部横排页签（最小高 54）、单列表单、输入框 16px、吸底页脚留安全区 | `style.css:355-418, 486-499` |
| 对话框 | ≤560px 按钮竖排、每个 44 高 | `style.css:992-995` |

---

## 11 待定项

以下为代码、设计画布、设计稿 v1.2 之间的不一致，以及代码内部的不一致。仅列出，不在本草案裁定。

### 11.1 尺寸

1. **悬浮球直径**：设计稿 v1.2 第 12 页写 44 圆；`host.css:29-30` 为 42×42（实测 42×42）；`Motion.dc.html:218` 与 `MiniStates.dc.html:40` 画的是 38×38、圆角 11 的方牌（方案 A）。
2. **PC 卡片最高**：设计稿 v1.2 第 3、12 页为 640；代码 `max-height: calc(100dvh - 16px)`（`style.css:644`），实测 805 高视口下上限 789。
3. **PC 播放键**：设计稿 v1.2 第 4、12 页为 56；代码 52（`style.css:824`）。
4. **页签高度与字号**：设计稿 v1.2 第 12 页 PC 高 34 / 12.5、触屏高 44 / 13；实测 PC 36 / 12、触屏 47 / 13（`style.css:612, 869`）。
5. **进度条**：设计稿 v1.2 第 12 页 PC 轨 6 / 旋钮 14、拖动时 10 / 18，触屏 10 / 22；代码 PC 轨 8 / 旋钮 14、拖动时不变，触屏 10 / 18（`style.css:735, 737, 742`）。
6. **触屏文字按钮**：设计稿 v1.2 第 12 页要求最小宽 88，代码无此约束；小窗两个按钮 44（`style.css:959`），其余 46。
7. **触屏标题字号**：设计稿 v1.2 第 12 页 16；代码悬浮窗标题 13（`style.css:522`），楼层行 14（`756`），无触屏覆盖。
8. **PC 列表行与药丸**：设计稿 v1.2 第 12 页 PC 列表行最小 40、药丸高 20 / 字 11；代码行无最小高（`style.css:778`），药丸 10.5（`794`）。
9. **悬浮窗宽度的多处取值**：画布 `Mini.dc.html:27` 为 344；`style.css:503` 为 344 后被 `745` 改为 360；脚本兜底宽度仍是 344（`index.js:10451, 11959`），兜底高度 360 与 320 两处不同（`10452, 11960`）。
10. **快捷行标签宽 34px**（`style.css:536`）只容两个字，画布用「模型 / 方案」（`Mini.dc.html:72, 79`）；现在标签是「翻译模型」（`index.js:10633`），实测折成两行（34×37）。
11. **控制中心 h1**：桌面 17（`style.css:44`，与 `Baseline.dc.html:96-97` 一致），≤640px 放大到 24（`style.css:374`）；≤900px 字标副题 6px（`style.css:346`）。`Baseline.dc.html:21` 自身标题用 19。

### 11.2 触屏交互与布局

12. **手机首次打开的尺寸**：设计稿 v1.2 第 7 页与 `README.md:558` 写「第一次打开是满屏」；代码默认 `compact`（`index.js:10524`）。`index.js:10583` 注释写「桌面窗口总是卡片」，与默认 `compact` 不符。
13. **手机卡片默认位置**：设计稿 v1.2 第 6 页为「屏幕下半区、离底 96px」；代码挂在入口旁（`index.js:10446-10473`）。
14. **触屏判定口径**：设计稿 v1.2 第 12 页按 `hover: none`；脚本按 `(max-width: 767px), (pointer: coarse)`（`index.js:10222`）；样式按 640 / 560 / 520 与 `hover: none`；`index.js:9170` 另用 `innerWidth <= 640`。641–767px 宽、精细指针的窗口会拿到 `data-touch="true"`，但 `style.css:658-665` 不生效。
15. **手机小窗的标题栏位置**：触屏下标题栏 `order: 1`（`style.css:871`），小窗概要没有设置 `order`（默认 0），于是概要排在标题栏上面（实测：概要顶 354，标题栏顶 454）。是否有意，未确认。
16. **触屏图标**：设计稿 v1.2 第 6、12 页规定触屏只用 ▶ ❚❚ ×；代码触屏标题栏为 ⤢ / ⤡、⚙、−（`index.js:10590-10592`），楼层箭头 ‹ ›（`10613, 10615`）。设计稿标题栏有「×」关闭（第 3、6、7 页），代码没有「×」，「−」即关闭回入口。
17. **每句按钮的默认值**：设计稿 v1.2 第 8 页为「触屏默认关、桌面默认开」；代码所有设备默认 `line`（每段一对，`core.js:610`），`sentence` 在触屏上也能选（`index.js:397`）。「触屏不放每句控件」目前只靠默认值保证。
18. **桌面卡片的动作区位置**：桌面内容区整页滚动（`style.css:646`），动作区排在段落列表之后（`index.js:10624-10631`），段落越多「翻译本楼 / 停止」越靠下（实测 5 段时动作区距页顶 444px）；触屏是列表自己滚动、动作区固定在底部（`style.css:891-894`）。两端行为不同，设计稿未规定动作区是否常驻可见。
19. **楼底栏触控目标**：实测手机上「朗读本楼」89×31、「缓存」43×25、「朗读面板」50×29，低于同楼段落按钮的 40px（`host.css:531`）与设计稿 v1.2 第 2 页「触控目标不小于 44px」。`.jy-tts-bar-pick`、`.jy-tts-bar-pick-all`、`.jy-tts-bar-pick-exit` 在 `host.css` 中没有样式（只有 `540-541` 两条），显示为宿主的默认按钮外观（实测为灰色方块）。
20. **句子行按钮数**：`style.css:961` 注释写三个按钮，实际四个「播放 / 重新生成 / 缓存 / 详细」（`index.js:11478`）；设计稿 v1.2 第 4 页为一个「改这句」。

### 11.3 颜色

21. **「停止」按钮对比度**：它同时是主要按钮（`index.js:10627`，底 `--jy-accent`）和红字（`style.css:533`），实测 #b54332 字在 #255d4e 底上对比度 1.38:1。`Baseline.dc.html:127` 的危险动作是描边按钮加红字。
22. **不随主题的字面色**：药丸 done / active / edited（`style.css:795-799`：#e3efe7、#fbf1dc、#8a6a2a）、出错日志底 #fbf3f1（`846`）。实测夜间主题下「✓ 深度分析」药丸为浅底 #e3efe7 配 #7fc4a4 字（对比度约 1.7:1），浮在深色卡片上。
23. **未定义的 token**：`--jy-danger`（`style.css:579`，回退 #c0392b）、`--jy-ink`（`982`，回退 #1f2a24）。`.jy-ask*` 的回退值（#f6f4ec、#5d6a63、#cfd6cf、#2f6b4f，`style.css:982-1010`）与真实 token 不同；调音台滑块回退为蓝色 #4c7cf3（`915`）。
24. **小对话框不跟主题**：询问 / 保存 / 重新分析三个宿主没有设置 `data-theme`（`index.js:4388-4391, 4437-4440, 5975-5978`），而主题选择器要求该属性（`style.css:422` 等），推断这些对话框始终是日间配色。未实测。
25. **「进行中」两种金色**：设计稿 v1.2 第 12 页定「进行中 = `--jy-focus`」；状态点用 `--jy-warn`（`style.css:113`），批次、段落、估时用 `--jy-focus`（`763, 768, 785`）；日间两者为 #b18537 与 #b08a3c；`Main.dc.html:83` 的运行点又是 #c29740。
26. **深色底固定色未 token 化**：见 6.3，这组颜色在 fresh、glass 等主题下保持不变，而 `--jy-rail` 会变。
27. **入口配色**：入口按主题换底色（`host.css:418-423`），但进度环轨道 #35604f（`157`）与 day 以外主题的标签色（仅 fresh 覆盖，`420-421`）不随主题。
28. **主题数量**：设计稿 v1.2 第 1 页写「沿用现有四款主题」；代码有五套（`core.js:1171`）。
29. **开关规格**：`Baseline.dc.html:136-142`、`Processing.dc.html:139, 149` 为 38×21、旋钮 15、关闭色 #c9d0c2；代码 42×25、旋钮 17、#bcc6b8（`style.css:149-152, 20`）。
30. **聚焦光晕浓度**：`Baseline.dc.html:132` 为 accent 14%；代码 16%（`style.css:161`）。
31. **按钮与徽章**：`Baseline.dc.html:124` 主要按钮 `padding: 0 15px`、字重 650；代码与次要按钮共用 `6px 13px`、600（`style.css:94`）。`Baseline.dc.html:146` 状态徽章 700、`--jy-accent-ink` 字；代码 `.jy-badge` 为 `--jy-muted` 字、无字重（`style.css:101`）。
32. **遮罩浓度**：`Motion.dc.html:228` 为 rgba(7,27,24,.62)；代码 #071b18b3（约 .70，`style.css:48`）。
33. **警示色块**：#faf3e4 底、#e6d5ae 边、#6d5522 字只出现在画布（`Motion.dc.html:194-197`，`Logs.dc.html:79`），代码没有对应 token。

### 11.4 圆角与字体

34. **圆角散值**：5 / 7 / 8 / 9 / 12 / 13 / 14 / 16 都是字面值（见第 4 节）。`Baseline.dc.html` 的卡片为 9（`94, 121, 155`），代码卡片为 10（`style.css:27`），`Main.dc.html:80` 为 10。
35. **字族**：小对话框用 `13.5px/1.6 system-ui, sans-serif`（`style.css:982`），与界面正文字族和 13 / 1.7 不同；等宽字族有三种写法（见 5.1）。

### 11.5 动效

36. **只动 transform / opacity**：`Motion.dc.html:110` 规定不动 width / height / box-shadow / backdrop-filter；代码动了入口的 width / border-radius / padding（`host.css:45-48`）、box-shadow（`51`）、悬浮窗 height（`style.css:648`）、抽屉 `grid-template-rows`（`259, 548`）。`Motion.dc.html:196` 建议的 `will-change` 在代码中没有使用。
37. **展开原点**：`Motion.dc.html:123` 为「origin 取球心」；代码固定 `transform-origin: 100% 100%`（`style.css:506`），窗挂在入口下方或左右时原点不在入口一侧。
38. **切页时长**：设计稿 v1.2 第 12 页切页 200ms、尺寸 240ms；代码切页 240ms（`style.css:653`）、高度 260ms（`648`）。
39. **控制中心关闭**：`Motion.dc.html:158-162` 为 150ms 退出曲线；代码立即移除（`index.js:10107`）。
40. **胶囊停留时长**：`canvas.json:98` 为 1.6 秒，`MiniStates.dc.html:71` 为「两秒」，代码默认 2200ms、关窗后 1800ms（`index.js:10213, 12066`）。
41. **按压反馈**：`Motion.dc.html:185-189` 列了「悬停 / 按下 120ms ease」；代码没有 `:active` 视觉。
42. **减少动态效果的两种写法并存**：`style.css:419` 全局 `none !important`，同文件 `336-340, 562-568, 907-909` 又写 1ms；`host.css` 入口用 1ms（`278-287`），楼层按钮用 none（`410-416`）。设计稿 v1.2 第 12 页要求「全关」，`Motion.dc.html:197` 写「降到 1ms」。
43. **无效声明**：`.jy-mini-tabs::after` 的滑动下划线（`style.css:651-652`）被 `749` 的 `content: none` 关掉；随手翻 ＋ / − 的 `transform` 过渡（`542`）没有对应的 transform 变化。
44. **曲线未命名**：`cubic-bezier(0.22, 0.72, 0.2, 1)` 用在 5 处、`cubic-bezier(0.3, 0.7, 0.4, 1)` 用在 1 处，没有对应 token；是否命名、叫什么，待常夜灯确定。

### 11.6 样式文件组织

45. **分层覆盖**：`style.css` 中同一组件有多段定义，后段覆盖前段：宽度（`503 → 745`）、动作区列（`531 → 805`）、传输键（`616-619 → 822-823`）、页签（`611-614 → 746-750`）、`.jy-close`（`54-55 → 472-473`）、`.jy-tts-voice-row`（`306 → 574 → 687`）、`.jy-tts-lang-block`（`589 → 667`）。读某个组件的最终值需要看完整个文件。

---

## 12 悬浮窗页面结构与楼层朗读入口（v0.34.0 起）

> 来源：`镜译-悬浮窗改版提案.md` 第二、三节，常夜灯 2026-09-23 批准，楼层入口选方案 A，按效果图确认。本节与第 2、9、10 节冲突处以本节为准。

### 12.1 页面结构

每一页都是一列：中间一块滚动，页面自己的操作固定在页脚，不常用的收进「更多」。窗口再矮也只是中间滚动，任何按钮都不会被裁掉。

| 区 | 内容 | 规则 |
|---|---|---|
| 滚动区 `.jy-mini-scroll` | 翻译页：状态 + 段落列表；朗读页：标题与楼层、状态胶囊、步骤、句子列表、改句面板；日志页：列表与详情 | 每页唯一的滚动区；`overscroll-behavior: contain`；内边距 `13px 14px 14px`，满屏 `14px 16px` |
| 更多 `.jy-mini-more` | 翻译页：翻译模型、方案、全翻、随手翻；朗读页：从头读、重新分析、保存到本地、定位到正文、复制分析 | 夹在滚动区和页脚之间，最高占 55%，自身滚动；页脚「更多」开合，右上 × 收起；底色 `--jy-band` |
| 页脚 `.jy-mini-foot` | 翻译页：朗读时的播放条 + 翻译本楼 / 停止、补译、重翻、自动、更多；朗读页：进度条、时间、停止、上一段、▶ / ❚❚、下一段、更多 | 不滚动；上边框 1px `--jy-line-soft`；内边距 `10px 14px 12px`；底色 `--jy-surface` |
| 日志页筛选 | 固定在滚动区上方 | 内边距 `12px 14px 8px` |

### 12.2 尺寸

| 尺寸 | 桌面 | 触屏 |
|---|---|---|
| `card` | 宽 360px，高 `min(640px, 100dvh - 16px)`（设计稿 v1.2 第 12 页的 640）；切页签不变高、不跳动 | 宽 92vw（最宽 400px），高 70dvh；键盘弹出时不超过可视视口 |
| `compact` | 宽 300px，高随内容 | 同左 |
| `max` | — | `100vw` × `100dvh` |

### 12.3 小窗（第一层）的按钮

小窗按当下状态只给要用的按钮，不展开就能停。

| 状态 | 显示 |
|---|---|
| 这一楼没翻译、也没在翻 | 翻译本楼（主按钮） |
| 正在翻译 | 停止翻译（危险按钮） |
| 正在朗读（准备中 / 播放 / 暂停） | ▶ / ❚❚（40px，触屏 44px）、停止朗读（危险按钮） |
| 开着朗读、没在读 | 读这一楼 |
| 任何时候 | 展开 |

### 12.4 按钮样式

| 项 | 规则 |
|---|---|
| 停止类（停止翻译、停止朗读、页脚「停止」） | 危险按钮 `.jy-mini-danger`：底色 `--jy-surface`，边框与文字 `--jy-error`；悬停底色 `--jy-error-soft`；不可用 45% 不透明 |
| 「更多」 | 文字按钮；图标仍只用 ▶ ❚❚ × |
| 传输区 | 按钮等分剩余宽度，间距 6px；▶ 52px（触屏 56px） |

### 12.5 触控目标（触屏）

标题栏按钮、楼层 ‹ ›、「更多」里的每一项、选择框、随手翻标题行、日志筛选、句子行按钮、面板关闭 ×：不小于 44px。进度条可视高度 10px，点按区上下各扩 17px。

### 12.6 句子行

行尾按钮（播放、重新生成、缓存、详细）默认收起。点一句：从这句读，同时展开这一行的按钮；正在读的那一行也展开。

### 12.7 进度与动效

| 项 | 规则 |
|---|---|
| 进度条 | 填充用 `transform: scaleX(var(--jy-seek))`，圆点在一条整宽轨道上用 `translateX` 移动，两者过渡 `120ms linear`；不对 width 做动画 |
| 刷新频率 | 朗读中时钟和进度每秒最多刷新 4 次；其余部分只在句子或状态变化时重画 |
| 循环动画 | 转圈加 `will-change: transform`，呼吸 / 脉冲加 `will-change: opacity` |

### 12.8 楼层朗读入口

| 项 | 规则 |
|---|---|
| 位置 | 楼层正文开头（`.mes_text` 的第一个子元素），不跟随滚动 |
| 内容 | 每个朗读语言一个按钮：「朗读」，读译文 + 原文时为「朗读译文」「朗读原文」；另有「缓存」进入勾选；勾选时为「已选 N 段」「缓存所选」「全选」「退出选择」 |
| 状态 | 播放中「暂停」，暂停中「继续」，准备中「准备中」（点按即停止）；状态文字放在悬浮窗，楼层里只作按钮的提示文字 |
| 尺寸 | 字号 0.8em，药丸内边距 `0.15em 0.7em`（触屏 `0.3em 0.85em`），与正文间距 0.55em；触屏点按区上下各扩 8px |
| 「楼层里的朗读按钮」选「不显示」 | 这一行隐藏 |
