# 验证边界

## v0.18.1 · 手机端与切页动画

- **手机端**（预览 375×812 模拟）：楼层每句后的播放 / 情绪按钮与底栏正常换行，`mes` 不含朗读数据；悬浮窗翻译页与朗读页均在视口内（朗读页高 600 以内、宽 344，超出时窗体内滚动）；朗读设置页在 355px 内无横向溢出，角色行按「名字 / 别名 → 锁定 → 音色 + 音色库 → 工具 → 语言音色」堆叠，音色库行按「名字 / 语言 → ID → 移除」堆叠。触控设备上播放 / 情绪按钮放大到 1.55em，朗读面板按钮最小 40px。
- **悬浮窗切页动画**：切页时 `.jy-mini` 加 `is-resizing` 并以行内高度从旧页高度过渡到新页高度（transitionend 或 420ms 兜底清除），新页带 `is-entering` 从所在一侧滑入淡入，页签下划线按 `data-mini-tab` 滑动；系统「减少动态效果」时全部跳过。预览窗格在后台时浏览器不推进动画帧，只验证了类名、行内高度与 CSS 过渡声明（`getAnimations` 列出高度过渡与页面入场动画），动画本身的观感未在前台核对。
- 177 项自动回归通过；打包后的副本单独验证通过。

## v0.18.0 · 朗读第二版

本地预览（模拟酒馆 + 模拟 Fish + 模拟副模型）和自动回归两处验证；没有安装到真实酒馆，也没有对真实 Fish 跑深度分析编译出来的标签。

- **自动回归**：177 项通过。新增覆盖：自定义对白 / 跳过符号切句与嵌套、按文字判断语言、深度分析请求与回复解析（词不在句中的停顿 / 重音 / 转折被丢弃）、Voice → Fish 标签编译（前置标签上限四个、句内停顿 / 强调 / 转折插入、句尾非语言声、S1 只用固定集合）、语速音量分档与按档拆请求、无音色时不带 `reference_id`、有无音色混排拒绝、按语言 / 锁定解析音色、录音覆盖表与跨单位查找（新的优先、改过的不冒充原句）、缓存键随改写变化、WAV 拼接、设置迁移（sentence → stream、model → auto）与音色库归一化；端到端：深度分析带角色卡与前几楼、按语速拆成两次请求、流式按段生成且换到全篇后不重复请求、整楼录音覆盖流式的每一句、无音色照常生成、改写单句生成 `sentence:N` 录音并被优先使用、恢复后不再冒充、后台逐段预生成且第二次为零、世界书识别（副模型 / 标题兜底）。
- **本地预览**（`npm run preview`）：翻译台开关打开后朗读页出现；朗读页填 Key、模型、旁白 / 对白默认音色，添加音色库条目并保存；「从世界书识别角色」导入樱井（别名桜井）与老胡两行，均为「跟随默认音色」；从音色库为樱井选音色后变为「🔒 专属音色」，加一行日语音色后保存，设置里 `voices.ja` 正确；翻译后楼层挂出 7 个播放按钮 + 7 个情绪按钮，底栏有暂停 / 停止 / 朗读面板，`mes` 不含朗读数据；「朗读本楼」触发深度分析（7 句，3 句带指令）并按语速拆成两段整楼音频，逐句高亮推进；暂停后底栏变「继续」、面板变 ▶；下一段跳到第 2 段第 2 句，上一段回到第 1 段；点情绪按钮打开悬浮窗朗读页，显示樱井那句的场景 / 情绪 / 心理 / 意图 / 人物状态 / 说话习惯、发给 Fish 的文本 `[happy] 你来了啊`；在光标处插入「叹气」并把语速改成 0.75 后「用这个重新生成」生成 `sentence:4` 录音并立即播放，「恢复自动」后回到自动版本；切到流式朗读后点第 6 句直接从整楼录音里播放，运行记录没有新的 `tts.recording`。
- **尚未验证**：真实 Fish 对深度分析编译出的自由文本标签（`[holding back]`、`[hesitant]`、`[hoarse]`）的实际效果；真实酒馆里世界书条目事件的时机；手机真机上悬浮窗朗读页的滚动；`drama-3-preview` 模型；切页动画在前台的实际观感。

## v0.17.1 · 朗读开关位置

预览中确认：翻译台「自动接续翻译」下方出现「朗读（有声小说）」一条，默认关闭、「朗读设置 →」隐藏、左侧无「朗读」页签，正文处理页不再有朗读勾选框；打开开关后页签出现、朗读页顶部同名开关同步勾选、「朗读设置 →」出现且点击后切到朗读页。166 项自动回归通过。

## v0.17.0 · 朗读

真实接口、宿主源码、本地预览三处分别验证；没有安装到真实酒馆里跑。

- **Fish 真实接口**（2026-09-14，作者提供的 Key 与公开音色，只在本地脚本中使用，未写入仓库）：
  - `/v1/tts/stream/with-timestamp` 为 `text/event-stream`，每帧 `event: message` + `data`，字段为 `audio_base64`、`content`、`alignment{segments,audio_duration}`、`chunk_seq`、`chunk_audio_offset_sec`。中文按单字对齐、去掉标点；`[happy]` 不出现在 segments 里，在 `content` 中被替换为空格；同一 chunk 的快照逐次变长，以最新一份为准。
  - 多说话人：`reference_id` 传数组 + `<|speaker:N|>` 在 `s2.1-pro-free` 上返回 200；换人处被切成新的 text chunk，第二段带独立 offset。
  - `/v1/tts` 直接返回 `audio/mpeg`；`/wallet/self/api-credit` 返回余额；无效 Key 返回 401 `Invalid token`；该账户 API 余额为 0，`s2-pro` 返回 402。
  - CORS：对 `api.fish.audio` 的 OPTIONS 预检返回 404 `no_route`，所有响应均无 `Access-Control-Allow-Origin`，浏览器无法直连。
  - 用扩展自己的适配层（`buildFishPayload` → `createSseParser` → `buildGlobalTimeline` → `alignSpansToTimeline`）对真实接口跑一楼旁白 + 对白 + 数字：5 句全部对齐，覆盖率 100%，总时长 12.63 秒，与 mp3 码率估算的 12.70 秒一致。
  - 测试用例 `test/tts.test.mjs` 中的对齐数据取自上述真实返回。
  - 日文原文（2026-09-15，同一 Key、`s2.1-pro-free`）：segments 按词切并保留汉字假名原文（`夕暮れ`、`教室`、`振り返っ`、`笑っ`），数字 123 拆成 `1`、`2`、`3`；6 句全部对齐、覆盖率 100%，总时长 12.35 秒。
- **SillyTavern `/proxy/` 源码审计**（release 分支 `src/middleware/corsProxy.js`、`src/server-main.js`、`src/util.js`）：代理需 `enableCorsProxy`；开启时对 `/proxy/` 跳过 CSRF，未开启时返回 404 `CORS proxy is disabled…`（CSRF 在其前面，因此请求需带 `X-CSRF-Token` 才能拿到这句提示）；`bodyParser.json` 在代理之前，请求体按 JSON 重新序列化转发；响应以流 pipe 回来，但不转发响应头，401 改写为 400。扩展据此自行补音频 MIME、按响应体里的 status 判断 401。Fish 在浏览器声明支持 zstd 时仍返回 gzip。
- **本地预览**（`npm run preview`，预览服务器模拟 `/proxy/https://api.fish.audio/…`，按说话人生成音调 WAV 与逐字时间戳）：翻译后楼层挂出 7 个按钮，对白按钮落在 `<q>` 之外，`mes` 不含任何朗读数据；整篇发送 1 次请求、7 句覆盖率 100%，再次调用命中 IndexedDB；点单句从句首前 0.08 秒播到窗口终点自动暂停；「朗读本楼」高亮随句推进、停止后清除；单句模式首次点击只请求 `/v1/tts` 一次，再点无请求；刷新页面后缓存仍在；测试连接、查询音色名、分析预览、导入与保存音色表均可用；可爱风、极简风、原文折叠三款美化下按钮均位于译文区、底栏样式随美化切换；华美吸血鬼与极简毛玻璃主题下「朗读」页配色正确。
- **一般模式与朗读语言**（预览）：默认关闭时控制中心只有 5 个页签、楼层无按钮；在正文处理页打开后朗读页签出现、朗读页上的同名开关同步勾选、楼层挂出 7 个按钮；切到原文后按钮移到日文原文行、底栏显示「读原文」、楼层 ID 带 `|source`、整篇音频 7 句覆盖率 100%；在朗读页关掉时页面只剩标题与开关、楼层按钮立即撤下，离开后页签隐藏。
- **自动回归**：166 项通过，新增 31 项，覆盖切句、分析请求与解析、标准结构、音色解析、情绪标签、Fish 请求体与拆段、SSE 断帧、快照覆盖、真实数据对齐、数字重同步、播放窗口、缓存键、错误提示、代理请求头、渲染后定位、来源标签回退、设置归一化、缓存淘汰与降级、Key 脱敏，以及楼层读取、分析缓存与失败回退、单句与整篇请求的端到端流程。
- **手机悬浮入口**（预览 375×812 手机模拟）：修复前贴边形态为 34×42、圆角 `999px 0 0 999px`，进度环实测 26×26 贴在核心区顶部，圆心比图标高 4px；修复后为 38×34、圆角 `10px 0 0 10px`、圆环隐藏，图标与条的纵向中线同为 455，点击检测确认条覆盖到 x=372（视口宽 375）；模拟翻译中 60% 时圆点变为 `#f0c56e`、底部细线 `scaleX(0.6)`；派发 resize 后切到桌面宽度，形态变回圆环、位置回到原先的 x=333。预览窗格模拟视口时不会触发 resize 事件，这一步是手动派发的。
- **尚未验证**：真实酒馆开启 `enableCorsProxy` 后的完整链路；超过「每次最多字数」的长楼层在真实 Fish 上的分段表现；iOS Safari 播放 opus；付费模型 s2-pro 的实际效果；真机上的悬浮贴边条。

## v0.12.0 · 本次改动

按作者要求，本轮只进行相关本地检查与模拟页面查看，未连接真实翻译 API，未安装到真实酒馆，也未进行多版本运行测试。

- 语法检查通过；核心与上下文的相关检查通过，正文方案的 8 项检查通过。
- 覆盖空前后缀、自然同名符号保留、主模型过滤、旧标记迁移、重复换样式、已有滑动页译文复用及保存失败回退。
- 覆盖原生正则完整字段、顺序与开关随方案往返，导入后重新分配 ID，隔离镜译规则组，保留其他原生规则。
- 模拟页面确认默认方案、紧凑管理行、底部折叠美化入口及方案切换。独立阅读预览使用同一份美化正则和样式，支持明暗切换与原文展开。
- 移除了清单最低版本限制；旧版脚本加载提供幂等启动回退。源码接口比对不等同于旧版酒馆实际运行通过。

原生正则接口依据 SillyTavern [1.18.0 的引擎](https://github.com/SillyTavern/SillyTavern/blob/8172dcd0ee672d3cd9a5e5f7af134f91a45cd2b8/public/scripts/extensions/regex/engine.js)、[管理器](https://github.com/SillyTavern/SillyTavern/blob/8172dcd0ee672d3cd9a5e5f7af134f91a45cd2b8/public/scripts/extensions/regex/index.js)及 [1.14.0 上下文](https://github.com/SillyTavern/SillyTavern/blob/9c9be90821ffd6132b40b5f04982522a61d7ad30/public/scripts/st-context.js)核对。使用 `extensionSettings.regex`、`saveSettingsDebounced()` 与原生显示正则；未依赖私有的正则列表刷新函数。路由资料为 2026-08-18 快照的 A0、D5、E5 与扩展/API 参考技能，未采用设计目录候选。

## 既有版本验证记录

以下是此前版本的证据与真实宿主验证参考，不代表本轮新增运行结果：

1. `validation/sillytavern-1.18.0.snapshot.json` 来自 SillyTavern `1.18.0` 标签源码审计，证明所调用的上下文符号和事件存在；
2. `npm run validate` 执行本地语法、核心行为、清单和能力契约检查。

这些检查不能替代真实宿主验收。后续有需要时可在目标 SillyTavern 中参考以下检查项：

- 从 Git URL 安装并刷新后扩展正常激活；
- 魔法棒入口与扩展设置入口均能打开控制中心；
- 控制中心不会在启动时自动遮挡聊天；
- 主页面、提示词页、设置页、日志页均可切换和保存；
- “跟随当前连接”和“独立副 API”各完成一次测试翻译；
- 新回复、切换已有滑动页、编辑、切换聊天期间的写回行为正确；
- 下一次主模型请求中不包含带不可见标记的中文镜像；
- 控制中心内的更新按钮能在真实 Git 安装中完成检查、安装和刷新；
- 禁用、启用、更新和清理扩展后没有重复事件或残留面板。

## 本轮本地验收

- 路由回执：`consult-tavernweave-library`、`sillytavern-extension-dev`、`code-quality-workflow`；资料快照 `2026-08-18`；采用 A0、D5、E5 的扩展契约、控制中心、表单和移动端边界；代码质量复核只返回 ST-A0；目录候选仍为 proposed，未采用；
- 目标：交付 0.8.0，在保留简体中文完整规则包的同时加入每方案目标语言、来源语言逐段自动识别、统一重试、标签检查和最新楼层安全合并；
- 红线：不依赖 TavernHelper；未复制参考插件的源代码、素材或视觉资产；提示词按已获授权的长规范与分层检查写法为正文翻译重新编写；程序负责的提取、排除、包裹、标记、写回和日志机制不进入模型提示词；模型的 `<thinking>` / `<think>` 内容不写进聊天正文；不在真实宿主验收前声称完全兼容；
- 数据迁移：设置结构升级为 schema 8；旧 `bodyTag` 自动迁为 `bodyTags`，默认值仍只有 `story_scene`；未修改过的 v0.6/v0.7 默认提示词升级为新版，用户自定义提示词继续保留；旧方案自动取得默认目标语言“简体中文”；
- 提取边界：每个已配置正文标签只选最后一组完整内容；不同标签共同参与同一次首次请求；标签输入接受名称或完整尖括号形式；排除块支持嵌套、自闭合和原位保留；相互交叉而无法安全还原的标签会明确报错；
- 分段边界：完整空行才创建新段落；段内换行、首尾空白和原文顺序均保留；每个空行段落只生成一个目标语言镜像块；
- 调用边界：正常首次翻译把全部选中正文一次发送，不按标签或段落拆批；返回不完整或命中绝对禁用表达时仍沿用恢复与定向修正流程，不伪造缺失译文；
- 并发写回：开始请求时记录的语义签名只包含实际待译段落；写回前重新读取最新楼层。排除标签内容或正文标签外内容变化时在最新结构上合并，真正待译正文变化时按统一预算重译；不使用固定等待或持续抢写；
- 自动回归：35 项通过，覆盖设置/提示词迁移、目标语言变量、非中文通用规则、多标签末组选择、标签检查、空行分段、段内换行、嵌套与自闭合排除、语义签名、整段前后缀、部分写回恢复、可见 think/thinking 解析、提示词职责边界、世界书上下文、参数排除、模型列表和生命周期契约；
- 本地浏览器结果：v0.8.0 控制中心可打开；主页面正确识别 2 段；提示词页 10 个标准条目默认全部收起且一次只展开一个，目标语言切到 English 后显示通用规则提示；设置页“检查当前楼层”报告 `<story_scene>` 1 组、采用第 1 组、2 个可译段落；副 API 请求中途模拟插入 `<image_prompt data-preview="late-insert"/>` 后标签原位保留、2 段译文完整写回，并产生 `translation.rebase` 日志；桌面对话框 client/scroll 宽度均为 761px；390×844 下对话框、内容和文本域无横向溢出；浏览器控制台无警告或错误；
- 通用扩展清单脚本会把带 `?v=0.8.0` 的 manifest 资源误判为非 `.js` / `.css` 文件；项目自身检查会去掉查询参数后核对真实文件，并保留查询参数解决更新后的混合缓存问题；
- 尚未关闭：真实 SillyTavern 中从 GitHub 更新到 0.8.0 后的入口恢复、真实副 API、真实世界书激活、滑动页/聊天切换保存流程，以及与实际文生图扩展同时写入同一楼层的验收。

## v0.8.1 本地验收

- 目标：修复独立副 API 拉取模型后的首次选择体验，并让空回、截断、格式漂移问题能够取得每次请求的完整返回证据；本轮没有修改翻译提示词、请求拆分、解析、重试或写回策略；
- 模型列表：拉取成功后不再把排序后的第一个模型写入当前预设；原生下拉框显示接口返回的全部模型，并保持“请选择”状态，用户明确选择后才同步到手动模型输入框；接口无法列出模型时仍可手动填写；
- 诊断记录：每次成功收到副 API 返回后，在解析前保存完整原始返回；对象响应保留 `content`、`reasoning`、`reasoning_content`、`thinking` 及其他原始字段，字符串响应原样保留；完整返回默认收起并可单条复制；
- 隐私边界：安全摘要不包含完整返回；完整日志明确提示可能含正文和模型思考，常见凭据字段与 Bearer/查询参数密钥形态会隐藏；达到本地存储预算时整条淘汰最旧记录，不截断仍保留的单条响应；
- 自动回归：36 项通过，新增完整返回保存、安全摘要隔离、凭据脱敏、模型列表不自动选择以及完整日志入口的覆盖；语法检查和项目清单检查通过；
- 本地浏览器结果：v0.8.1 模拟宿主中拉取到 `translator-pro`、`translator-small` 后，两项同时可见且没有自动选中；选择 `translator-small` 后手动输入框同步；测试请求生成 `translation.raw-response` 日志，展开后可见完整 `content` 与 `reasoning`；
- 已知验证边界：上述为模拟宿主和模拟 OpenAI 兼容端点的本地证据，尚未证明真实 SillyTavern 安装更新、真实副 API 的思考字段形态或浏览器存储配额表现。跟随当前连接时，扩展只能记录 SillyTavern `generateRaw` 实际返回给扩展的字符串；宿主未暴露的隐藏思考无法由扩展补取。

## v0.8.2 本地验收

- 目标：把平行线、控制标签、装饰线和段内换行的机械处理从副模型提示词移回脚本，同时保持正常首次翻译只有一次整批请求；
- 数据迁移：设置结构升级为 schema 9，旧设置原样保留并补入空白的“原样保留白名单”；未修改翻译提示词及用户自定义方案；
- 分段与写回：完整空行仍决定外层段落；段内语义行使用连续内部 ID，一次发送给副 API，返回后按原行序组合进同一个多行镜像块；新镜像块使用成对不可见边界，生成拦截、重新翻译和部分恢复均能完整识别；旧版单行不可见标记继续支持剥离与单行恢复；
- 结构保护：排除标签整块原样保留；其他转义或未转义标签视作透明结构，标签语法不进入翻译单元；纯符号、纯边框和仅含标签的行由内置规则原样保留；
- 自定义白名单：支持整行、`prefix:` 行首和 `/正则/flags` 三种规则；命中行不建立翻译 ID、不发送给副 API且保持原位置；错误正则和空 prefix 会给出行号；暂不支持任意行内子串白名单，行内整块排除继续使用排除标签；
- 检查报告：显示空行段落数、实际翻译行数、自定义白名单命中、内置结构保留行及透明结构标签；
- 自动回归：38 项通过，新增多行镜像剥离/恢复、旧单行标记兼容、白名单三类规则、无效规则、透明容器标签、转义排除标签以及容器闭合位置覆盖；语法、清单一致性和 diff whitespace 检查通过；
- 尚未关闭：真实 SillyTavern 1.18.0 中的更新重载、真实长正文副 API、平行线预设、移动端设置页，以及与实际文生图扩展同时写入同一楼层的联动验收。

## v0.9.0 交付

- 重写通用规范和清单，采用全篇简短核对，取消逐 ID 检查报告；保留单批请求和现有解析协议。
- 旧默认文本按完整内容匹配升级，用户修改不自动覆盖；提供带方案备份的手动采用入口。
- 界面改为紧凑深色浅紫风格，提示词列表默认收起；新增模型搜索和首页当前方案入口。
- 按本轮要求只做语法及现有核心检查；真实模型翻译质量、桌面/手机界面和交互由人工验收。

## v0.10.0 前端重建

- 重写静态界面与整个面板样式，改用五个工作区；提示词条目编辑器移动到独立区域并保留编辑位置。
- 对照上一版本静态界面的 action、设置字段和连接字段，三类控制均无遗漏。
- 本地模拟宿主快速查看了桌面工作台及窄屏规则编辑，修正编辑聚焦引起的外层窗口滚动；正式酒馆和人工操作体验待验收。
- 提示词文本、翻译调用、提取/写回算法没有修改。公开版本 0.10.0。

- 追加五主题、方案导入导出、更新入口与移动端留边调整后，按用户明确要求停止浏览器操作，仅做 JavaScript 语法与文件差异检查；追加部分的外观、导入导出和更新交互由用户验收。
