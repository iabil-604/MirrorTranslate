# 镜译 · 使用手册

这份手册只讲怎么用，不讲原理。按顺序做就行，每一步都说了在哪点、填什么。

镜译是 SillyTavern（酒馆）的扩展，做两件事：

1. **翻译**：把 AI 回复里的正文翻成中文（或你指定的语言），贴在原文下面。主模型看不到译文，不会被译文带偏。
2. **朗读**：把译文或原文念出来，旁白和每个角色各用各的声音，副模型给每句配情绪。这个功能默认关着，不用可以完全不管。

---

## 一、装上它

**方法 A：用 Git 地址装（推荐，以后能一键更新）**

1. 酒馆顶栏点「扩展」（积木图标）。
2. 点「Install extension」（安装扩展）。
3. 粘贴这个地址：`https://github.com/iabil-604/MirrorTranslate`
4. 点安装，等它装完。
5. 刷新酒馆页面（F5）。

**方法 B：用压缩包装**

1. 解压，得到一个叫 `MirrorTranslate` 的文件夹。
2. 把它整个放进酒馆目录下的 `public/scripts/extensions/third-party/` 里。
3. 刷新酒馆页面。

**怎么确认装好了**

- 输入框左边的魔法棒菜单里多了一项「打开镜译 · 正文翻译器」。
- 屏幕角落多了一个带铅笔的圆球（悬浮球）。
- 「扩展」面板里多了一栏「镜译 · 正文翻译器」，里面有「打开控制中心」按钮。

这三个入口打开的都是同一个「控制中心」。

---

## 二、第一次用：三步

### 第 1 步：连上翻译模型

控制中心 → 左边「模型连接」。

两种选法：

- **跟随酒馆**：用你现在聊天用的那个模型翻译。什么都不用填，选它就行。
- **独立副 API**：单独给翻译配一个模型（更便宜的、更擅长翻译的都行）。选它以后：
  1. 「API 基础地址」填服务商给你的地址（形如 `https://xxx/v1`）。
  2. 「API 密钥」填你的 key。
  3. 点「拉取模型」，在列表里选一个；拉不出来就在「当前模型」手动填模型名。
  4. 点右上角「测试连接」，看到成功再点「保存连接」。

> 分不清选哪个？先选「跟随酒馆」，能用了再研究独立副 API。

### 第 2 步：告诉它正文在哪

控制中心 → 左边「正文处理」。

预设通常会把正文包在某个标签里，比如 `<story_scene>…</story_scene>`。镜译只翻标签里面的内容，标签外面的状态栏、思维链一律不碰。

1. 「提取标签」里填标签名，默认是 `story_scene`。你的预设用别的标签，就改成那个名字（不带尖括号）。不知道用的什么标签？打开一楼 AI 回复的编辑框看一眼。
2. 点右上角「检查当前楼层」。它会告诉你认出了几个段落。认不出就是标签名填错了。
3. 点底部「保存设置」。

### 第 3 步：翻一楼试试

控制中心 → 左边「翻译台」→ 点「翻译当前回复」。

等一会儿，最新一楼的每段原文下面就会出现译文。第一次能翻出来，后面就都是自动的了。

---

## 三、平时怎么用

装好以后你几乎不用再开控制中心：

- **自动翻译**：翻译台上的「自动接续翻译」默认是开着的。AI 每回一楼，镜译自动跟着翻。
- **切换滑动页时补译**：你在几个回复之间左右滑动时，没译过的那页会自动补上。
- **编辑回复后自动重译**：在「正文处理」页里，默认关着。开了以后你改完原文它就重翻。
- **悬浮球**：可以拖到任何地方。点一下打开悬浮窗，标题栏写着当前楼和状态（第 12 楼 · 已译 / 翻译中 / 缺 2 段 / 未译），窗也能拖，拖到哪记到哪。里面三页：
  - **翻译页**：这一楼一段一行，灰字是原文，下面是译文，行首 ✓ 是译好了、! 是缺失、◌ 是翻译中。点一行展开：译文可以直接改，点「写回」只改这一段；「重翻这段」只把这一段发给副模型；「补这段」补缺的那段；开着朗读还有「试听」。底下「翻译本楼」「重翻」「补译」「停止」，「自动 开 / 关」是新楼自动翻译的开关。还有「还有 N 楼没翻 · 全翻」，把这个聊天里没翻的楼一次翻完。标题旁边的 ‹ › 换楼。翻译进行中能看到分了几批、做到第几批、还要约几秒，副模型在想什么。
  - 「随手翻」：粘一段文字进去直接翻，结果只显示在小窗里，不写进聊天。Ctrl + Enter 直接翻。
  - 「模型」「方案」两个下拉快速切换翻译模型和翻译方案。
  - **朗读页**：开了朗读才有，见第六节。
  - **日志页**：本楼 / 全部 / 翻译 / 朗读 / 只看出错，点一条看细节和请求返回，「复制全部」给作者反馈时直接贴。
- **译文不会污染主模型**：主模型收到的上下文里只有原文。你不用管这件事，它自动的。

翻错了、漏了几段？翻译台上：

- 「翻译当前回复」：整楼重翻一遍。
- 「补译缺失段落」：只补没译的段落，已经译好的不动。

---

## 四、翻译规则（想改文风的时候看）

控制中心 → 左边「翻译规则」。

- **方案**：一整套翻译要求叫一个方案。默认方案叫「中文轻小说」，够用。
- **目标语言**：想翻成别的语言，改这里。
- **标准条目**：左边目录里的每一项都是一条规则（文风、译名怎么处理、敬称、标点……）。点一项，右边能改。改过的会标出来，「恢复当前方案」能改回去。
- **自定义条目**：点「＋」加你自己的要求，比如「XXX 这个角色说话要冷淡」。
- **术语表**：固定某些词的译法，一行一个。
- **参考资料与上下文**：翻译时要不要把世界书、角色卡、前几楼对话一起发给翻译模型。发了译名更准，但费 token。
- **方案管理**：导出 JSON 分享给别人，或者导入别人的方案。
- 改完记得点底部「保存方案」。

---

## 五、正文处理（想改译文长什么样的时候看）

控制中心 → 左边「正文处理」。

- **提取标签**：见第二节。
- **替换标签**：这种标签里的内容翻完以后，译文直接顶替原文显示，原文藏起来（点楼层的小铅笔能看到）。想只看译文不看原文就用这个。
- **排除标签**：这里面的内容原样保留，不翻。
- **段落前后缀**：给原文、译文加固定的符号，默认译文包在 `{ }` 里。可以留空。
- **每行单独成段**：勾上后原文和译文一行对一行贴在一起。
- **内置美化**：三款现成的样式（可爱风、极简风、原文折叠），选一个点「使用」。
- **说话人着色与情绪排版**：让每个角色的台词有自己的颜色，情绪不同字重、斜体不同。用法：
  1. 先点「读取当前主题与壁纸」（换主题、换壁纸后要再点一次）。
  2. 勾「说话人着色」。
  3. 点「添加角色」，填角色名和发色。没登记的角色会按名字自动分一个颜色。
- **显示悬浮入口 / 悬浮入口形态**：不想要悬浮球就在这关。
- 改完点底部「保存设置」。

---

## 六、朗读（有声小说）

### 先准备两样东西

**1. Fish Audio 的 API Key**

1. 打开 fish.audio，注册登录。
2. 进开发者页面（fish.audio/app/developers），创建一个 API Key，复制下来。
3. 注意：**API 的额度和网页端的额度是分开的**。API 余额是 0 的话，只有 `s2.1-pro-free` 这个免费档能用；想用付费模型要在开发者页面充值。

**2. 打开酒馆的代理开关（必须做，只做一次）**

Fish 的接口不允许浏览器直接访问，要让酒馆帮忙转发。

1. 找到酒馆目录下的 `config.yaml`。
2. 找到 `enableCorsProxy`，把它改成 `true`。
3. 重启酒馆。

没做这一步，朗读时会提示「酒馆的 CORS 代理没有打开」。

**用 TauriTavern（TT 酒馆）的看这里。** TT 酒馆没有 `config.yaml`，也没有酒馆那条通用代理，上面这步不存在。镜译认得出自己在 TT 里，会把「经酒馆 CORS 代理发送」变灰、把请求直接发出去。但 api.fish.audio 不让网页直接访问，所以要自己架一个带跨域头的转发，把地址填进「接口地址」。最省事的是 Cloudflare Worker，免费额度够用：

1. 打开 dash.cloudflare.com → Workers & Pages → 创建一个 Worker，把下面的脚本整个贴进去替换默认内容，部署。
2. 部署完会得到一个 `https://xxx.workers.dev` 的地址，填进镜译朗读页「Fish Audio」栏的「接口地址」。workers.dev 在国内可能打不开，绑一个自己的域名就好。
3. 点「测试连接」，能显示余额就通了。Key 还是填在镜译里，Worker 只是原样转发，不存任何东西。

```js
export default {
  async fetch(request) {
    const cors = {
      'Access-Control-Allow-Origin': '*',
      'Access-Control-Allow-Methods': 'GET, POST, OPTIONS',
      'Access-Control-Allow-Headers': 'Authorization, Content-Type, model',
    };
    if (request.method === 'OPTIONS') return new Response(null, { status: 204, headers: cors });
    const url = new URL(request.url);
    const upstream = new Request('https://api.fish.audio' + url.pathname + url.search, request);
    upstream.headers.delete('Origin');
    const response = await fetch(upstream);
    const headers = new Headers(response.headers);
    for (const [key, value] of Object.entries(cors)) headers.set(key, value);
    return new Response(response.body, { status: response.status, headers });
  },
};
```

Worker 的地址谁拿到都能拿去转发（没有 Key 转发了也没用，Key 各人自己的），介意的话在脚本开头加一句校验自己的口令。等 TauriTavern 加上通用代理，这一步就不用做了。

### 打开朗读

1. 控制中心 → 「翻译台」→ 找到「朗读（有声小说）」这一条，把开关打开。
2. 左边会多出「朗读」页，点开关旁边的「朗读设置 →」直接进去。朗读页分四栏（读什么、Fish Audio、音色、音色库），每栏都能点标题折叠，折起来的标题后面写着里面填了什么。
3. 「Fish Audio」这一栏：
   - 「API Key」填刚才复制的 key。
   - 「模型」：没充值就选 `s2.1-pro-free`；充了值选 `s2-pro` 或 `s2.1-pro`。
   - 「经酒馆 CORS 代理发送」保持勾选。TT 酒馆里这一项是灰的，不用管，「接口地址」填你的转发地址。
   - 点右上角「测试连接」，会显示余额。
4. 点底部「保存朗读设置」。

现在每一楼译文的每个自然段后面都会出现两个按钮：**播放**（从这一段读起）和**重新生成**（丢掉这一段的音频再向 Fish 要一次）。楼层底部多一条「朗读本楼」。电脑和手机都有。想要每句一个按钮，「正文处理」页的「楼层里的朗读按钮」改成「每段一个，再加每句一个」。

### 给角色配声音

声音就是 Fish 的「音色」。每个音色有一串 32 位的 ID，在 fish.audio 音色页面的**地址栏**里就能看到。

朗读页「音色」这一栏：

- **旁白音色**：读叙述部分用的声音。
- **对白默认音色**：没有单独配声音的角色都用这个。
- **角色表**：给每个角色单独配。三种加法：
  - 「从角色卡和世界书识别角色」：让副模型读一遍角色卡和世界书，把人物一次全导进来。**最省事，先点这个。** 它需要翻译模型能连上（第二节第 1 步那个）；酒馆没连模型、独立副 API 没配好的时候会直接报错，什么都不加。导错了点「清空角色表」重来。
  - 「导入已知说话人」：把着色名单里、翻译时认出来的名字导进来。
  - 「添加角色」：手动填。
- 导进来的角色一开始都是「跟随默认音色」：你改对白默认音色，它们一起变。给某个角色填了专属 Voice ID，它就变成「🔒 专属音色」，以后改默认音色不影响它。想让它重新跟随默认，点「解除绑定」。
- 角色表里每个角色折叠成一行，点名字展开编辑。导错了：「删掉没绑音色的行」只删没配声音的，配好的留着；「清空角色表」全清。
- **角色表保存范围**：默认「跟着角色卡」，同一张卡的所有聊天共用一张表。同一张卡开了好几个周目、每个周目人物不一样，就改成「每个聊天单独一份」：新聊天一开始借用角色卡那张表，你在这个聊天里改动并保存后，它就有了自己的一份，别的聊天不受影响。音色库不受这个影响，永远全局共用。
- **多国语言**：同一个角色读中文用一个声音、读日语原文用另一个声音，见下面单独一节。
- **音色库**（下面一栏）：把常用的音色 ID 起个名字存起来，以后每个音色框旁边的「从音色库选…」下拉就能直接选，不用再翻 ID。
- 「查询音色名」：把你填的 ID 对应的音色名字从 Fish 拉下来显示，方便核对。

> 没配声音的角色也会读，用对白默认音色。连默认音色都没填，就用 Fish 自己的默认声音。不会因为没配声音就不读。

### 多国语言：一个角色几种声音

一个角色读中文用一个音色、读英语用另一个、读日语原文再换一个，都能设。英语还分三档：英语（通用）、美式英语、英式英语（伦敦腔）。德语、法语、西班牙语、俄语、韩语等各一档。

给角色加：

1. 朗读页 → 「音色」→ 角色表里找到那个角色。
2. 点它那一行的「＋ 多国语言音色」按钮，下面多出一行。
3. 左边选语言，右边填这门语言用的 Voice ID，或者从「从音色库选…」里选。
4. 要几门语言就点几次「＋ 多国语言音色」。
5. 点底部「保存朗读设置」。

给旁白加：「音色」栏里有一块叫「多国语言」的区域，点「＋ 旁白加一门语言」，其余一样。

怎么生效：

- 每句话是什么语言，由副模型判断（角色设定里写了「美式口音」「英国人」这类，它会标成美式或英式）；没判断时按文字本身（假名算日语、汉字算中文、拉丁字母算英语）。
- 找音色的顺序：先找这句语言精确对应的（比如英式），没有就找通用英语的，再没有就用这个角色绑过的任意一种英语，都没有就用角色自己的专属音色，再没有就用对白默认音色。
- 所以最简单的用法：给角色绑一个中文音色、一个日语音色，读译文和读原文自动各用各的。

### 三种模式，选一个

朗读页最上面的「分析模式」下拉框。先说共同点：音频都是一段一次 Fish 请求，几段一起发（「Fish Audio」栏里的「同时生成几段」），先到先播；点某一句只做它所在的那一段；分析结果和音频都存在本地，正文没变就不再请求。差别只在「谁来决定这句怎么念」：

- **不分析**：一次副模型都不请求。正文按引号分成旁白和对白，谁在说沿用翻译时标好的说话人（没标过就用对白默认音色）。发给 Fish 的只有正文本身，加上调音台里配的「标点情绪标签」（见「角色调音台」）。最快、最省，适合日常听、手机、中文正文不开翻译的人。
- **简单分析**：副模型只做三件事：判断每一句谁在说、什么情绪、有没有明显的语气和声音。用的全是 Fish 官方认得的英文标签，一句一个情绪词，最多再加一个语气（小声 / 耳语 / 喊 / 急促）和一个声音（叹气 / 笑 / 喘息……），不发自由发挥的中文指令，因为实测那类标签容易读出怪气音。开着翻译时翻译顺手就标好了，零次额外调用；不开翻译的楼按播放时问你一下（下面说）。标点情绪标签在这个模式里也生效。
- **深度分析**：暂时灰掉了，选不了。思路是在简单分析上再问一次副模型，结合角色卡、世界书、前几楼和角色关系判断「为什么这样说、该怎么演」；成品质量还不够好，等迭代好再放开。以前存过深度的设置自动回到简单。

**没分析过的楼，按播放时问一下。** 简单模式下，一楼既没有翻译骨架、也没分析过（典型：中文正文、不开翻译），你点某句播放或「朗读本楼」时会弹一个小框：

- 「先分析再读」：整楼简单分析一次，之后这一楼都用它，再点别的句子直接读。
- 「直接读」：这一楼按不分析读，之后这一楼不再问。
- 「取消」：什么都不做。
- 勾「以后都这样，不再问」就变成设置，朗读页「没分析过的楼，按播放时」能改回来（问我一下 / 先整楼简单分析再读 / 直接读）。

分析什么时候发生：只在你按播放、点单句、「朗读本楼」、「重新分析」，或者勾了「最新一楼翻译完成后自动生成音频」的时候。打开悬浮窗看句子列表、开详细页都不会请求；没分析过的楼，列表上方会写「还没分析」，详细页里显示的是翻译骨架或正文本身。

### 译文和原文一起读

「读什么」里的「朗读语言」选「译文 + 原文」：细读只做一次（做译文那边），原文那边自动套用同样的说话人和情绪；两种语言各自生成，原文的每段和译文的每段后面各有一对按钮，点哪个读哪个。底栏会变成「朗读译文」「朗读原文」两个按钮。给角色绑了中文和日语两个音色的，正好各用各的。

### 生成了先不播

「读什么」里把「生成完自动播放」关掉：点句子只生成不播，再点一次才播；「朗读本楼」先把整楼做好，再点一次才从头播。适合出字快、只想听某几句的人。

### 楼层上的按钮

- **播放（每段后面）**：从这一段读起，然后一直往下读。这一段已经生成过就直接播，没生成过就只生成这一段（朗读的单位本来就是一个自然段，点它不多花一次调用）。
- **重新生成（每段后面）**：丢掉这一段的音频，再向 Fish 要一次，然后播。同一段文字 Fish 每次读得不一样，想换一个版本就点它。
- 这两个按钮**电脑手机都有**，一段一对，够大，点得准。「正文处理」页的「楼层里的朗读按钮」能改：每段一个（默认）／每段一个再加每句一个／不显示。
- **▶ 和情绪按钮（每句后面）**：选了「再加每句一个」才有。▶ 只播这一句、播完就停；情绪按钮打开悬浮窗的「朗读」页，看这一句被分析成什么、改它，见下面。
- **朗读本楼**：从头读整楼。读的时候正在读的句子会高亮。
- **暂停 / 继续、停止**：读的时候出现在楼层底部。
- **朗读面板**：打开悬浮窗的「朗读」页。

### 悬浮窗

点悬浮球先打开**小窗**：楼号和状态、正在做的事、正在读的播放条、「读这一楼」和「展开」。点「展开」是三页的卡片（翻译 / 朗读 / 日志），手机上再点右上角是满屏。上次的大小会记住，右上角 ⤢ / ⤡ 在几种大小之间切。

**「朗读」页：**

- **播放器**：标题是正在读的说话人和那句话，下面进度条。**进度条能拖**：点一下跳过去，按住拖着走，键盘左右键每次 5%。播放中、暂停中都能拖；读完了再拖，就从那一句重新读。两端是时间。
- **上一段 / ▶ / 下一段**：中间那个大圆钮是播放和暂停；没在读的时候点它从头读这一楼；详细页开着时点它读那一句。
- **三颗药丸**：分析做没做、生成到第几段、走的哪条通道。还在准备的时候标题下面会写「还要约 N 秒」。生成中间还会展开一张步骤清单，做完就收起来。
- **文字链接**：从头读、重新分析（见下）、保存到本地（点一下变「确认保存?」，再点一下才存）、定位到正文（把聊天滚到正在读的句子）、复制分析（每句的说话人、情绪、标签和发给 Fish 的文本，JSON）。
- **句子列表**：一句一行，✓ 是已有音频，· 是还没做，♪ 是正在读。每行末尾三个按钮：
  - **播放**：从这句读起（点整行也行）。
  - **重新生成**：丢掉这句所在那段的音频，再向 Fish 要一次。改了调音台、换了音色、想再抽一次都用它。
  - **详细**：进这句的页面，见下。
- **详细页**：
  - 译文 + 原文一起读的人，最上面是这句的原文；下面是谁说的、用的哪个音色、分析出来的情绪、语气、声音。
  - 「发给 Fish 的内容」：最终送去合成的文本，方括号里是情绪、语气、停顿、声音的标签。**可以直接改**。
  - 想在某个词前面加停顿、强调、叹气、耳语……把光标点到那个位置，再点下面的标签按钮，标签就插进去了。「插入情绪…」下拉里有六十多种情绪。
  - 「自己写一条指令」：输入框里打字（比如「压低声音」），回车或点「插入」就以方括号插到光标处。写什么 Fish 都会照读，但只有它官方认得的词最稳。
  - 「语速」「音量 dB」也能改。
  - 改过字才出现「重新生成并播放」，只重做这一句，马上播给你听；以后整楼朗读也用你改的版本。
  - 「恢复自动」：不要我改的了，回到分析的版本。右上角 × 回到句子列表。
  - 手机上键盘弹起来时，窗口会跟着缩到键盘上面，正在改的框自动滚到中间，按钮不会被键盘盖住。
- **长楼先出声**：一楼超过一批（默认 12 句）的，副模型分几批分析，第一批回来就先生成、先播，后面的批次边播边来，下一段在读上一段时就预取好。步骤清单里分析那一行会写「已回 1/3 批」。
- **发给 Fish 的长什么样**：`[angry][shouting] 骗人的吧！！这 [break] 不太好吧 [chuckling]` 这样：句首一个情绪词、可选一个语气，句内是标点换来的标签，句尾可以有一个声音，全是 Fish 官方认得的英文词。S1 模型用它的括号写法 `(angry)`。

### 分析结果不满意：重新分析

朗读页的「重新分析」，还有详细页里的「改这一句的分析」，都走同一个小窗（只有已经分析过、或者翻译时标过骨架的楼才会弹，第一次分析不问）：

- **范围**：只改这一句 / 整楼都改。从详细页进来默认只改这一句。
- **哪里不对**：写一句话，或者点下面五个常见说法（说话人不对 / 情绪不够 / 情绪太过 / 语气太平 / 声音太多）。可以不写。
- **按意见改**：副模型拿到的是上一次的标注加你这句话，不用再通读正文，只改你说到的地方；没说到的句子原样保留。改一句就是一个很短的请求，比整楼快很多，也便宜。
- **丢掉重来（整楼）**：整楼从头分析，和以前一样。

改过的句子音频会被丢掉，下次播放时按新的标注重做；没改的句子的音频留着。

**换角色音色不用重新分析。** 分析里只有谁在说、什么情绪，没有音色 ID；音色是发请求那一刻按名字查表的。换音色只会让那些句子的音频重做一次，不会再调副模型。

### 其他设置（朗读页「读什么」那一栏）

- **分析模式**：不分析 / 简单分析 / 深度分析（灰掉），见上面「三种模式」。
- **没分析过的楼，按播放时**：问我一下 / 先整楼简单分析再读 / 直接读，不分析。
- **朗读语言**：读译文，还是读原文（日语音色读日文原文很合适）。
- **朗读范围**：旁白 + 对白 / 只读对白 / 只读旁白。
- **翻译骨架是什么**：朗读开着（不分析模式除外）的时候，翻译请求里会多一节要求，让翻译模型顺手给每段、每句台词标上说话人、情绪词（Fish 官方列表里的）、可选的语气和声音。这些存在楼层的元数据里，不进正文，翻完就有。开朗读之前翻的楼没有骨架，简单模式按播放时会问你要不要分析一次。
- **发给 Fish 前去掉正文里的 HTML**：默认开。预设或者「说话人着色」给正文加的颜色、字号、加粗这些标签只留在页面上，发给 Fish 的副本只有字；`<br>` 变成换行。
- **分析用的副 API**：深度润色、简单分析、从世界书识别角色走哪条通道。默认跟随翻译的模型设置；想快、想省，先在翻译页「独立副 API」里加一条快模型的通道，再在这里选它。分批的时候几批同时发由那条通道的「并发数」决定，每批带上前面三句当语气参考。
- **分析每批句数**：默认 12 句，可选 8、20 或不分批。每批回来就先读这一批，等得短；但每批都带完整的背景资料，批数就是调用次数。不分批就是整楼一次，等得最久、只调一次。
- **连续的！！！压成一个**：默认开。「！！！」发给 Fish 时变成一个，强度交给情绪标签，不然容易鬼叫。
- **默认调音台**：见下一节。
- **副模型提示词（高级）**：深度和简单两套提示词都能改，留空用内置。「填入内置的再改」把内置的填进框里让你改，「复制内置提示词」直接复制走。JSON 格式和字段名别动。
- **对白符号 / 跳过符号**：告诉它哪些符号里是台词、哪些符号里的东西不要读。比如你的预设把动作写在 `*星号*` 里、台词写在 `“引号”` 里，就在「跳过符号」填 `* *`，这样 `樱井说：“明天也来吗？” *低头摆弄着衣角*` 只会读引号里的话。
- **最新一楼翻译完成后自动生成音频，不播放**：勾上后每楼翻完就先把音频做好，想听的时候点一下就播，不用等。只做最新一楼。
- **「保存到本地」保存什么**：整楼音频（默认），还是正在读的那一段。
- **深度模式时附带**：角色卡 / 世界书 / 前几楼。不想让副模型看这些就取消勾选。「前几楼」还决定要不要把各角色上一句的语气一起发过去。
- 「声音参数」里能改音频格式（默认 mp3，兼容最好）、语速、音量等。
- **超时 / 重试 / 同时生成几段**（「Fish Audio」那一栏，不在「声音参数」里）：超时是一次请求等多久；重试是超时、断线或者 Fish 自己出错（5xx）时再要几次，默认 1 次（Key 不对、余额不足、限流这些一次就停，重试没用）；同时生成几段是几段一起发给 Fish，默认 2，读的时候后面的段就是按这个数并行预取的，Fish 提示限流（429）就调回 1。这三项不影响声音，改了不会让已经生成的音频重做。

### 角色调音台

每个角色可以有自己的一套声音习惯。朗读页「音色」那栏，展开一个角色，下面有「调音台」：

- 七个滑块：**停顿感、气息感、口语颗粒度、情感强度、情绪表现幅度、语速倾向、声音表现倾向**。中间是平常，什么都不说；拉到两头才会变成一句话交给副模型，比如气息感拉高就是「呼吸感明显但自然：紧张、害羞、疲惫、犹豫时可以加入呼吸或喘息，不要每句都加」。数字本身不发给任何模型，Fish 也看不到。语速倾向那个滑块另外还会直接变成语速参数（副模型没说快慢的句子；不分析模式下每句都是），拉高读得快一点，拉低慢一点。
- **自定义配音规则**：一行一条，原样交给副模型，比如「害羞时不要过度娇柔」「生气时保持克制」「不要机械地每句话都加呼吸」。
- **标点情绪标签**（折叠在调音台底下）：正文里出现左边的标点，发给 Fish 的文本里就加右边的标签。左边填标点（……、！！、？！、——），右边下拉选标签，再选位置：「原位替换」把标点换成标签，适合停顿、叹气这类声音；「放在句首」把标签放到这个标点结尾的那句话前面，适合情绪、音量。「填入推荐搭配」一键给三条：……→停顿、！！→加大音量、？！→惊讶。发出去的是 Fish 官方认得的英文标签，不分析和简单两种模式都生效。角色没单独配就用默认调音台的那套。
- 没单独调过的角色和旁白，用「读什么」那栏里的**默认调音台**。
- 改了调音台或标签，这一楼下次播放会重新生成，同一个音色能听出不同效果。想立刻听，句子行尾点「重新生成」。

### 关于重复调用

生成过的音频都存在你浏览器里（不进聊天记录）。同一句话、同一个音色、同样的声音参数，不会再次调用 Fish，不管你是点单句、读整楼还是换了模式。以下情况会重新生成：正文改了、换了音色、改了声音参数、你在朗读面板里改了这句的内容。朗读页最底下能看到缓存占了多少空间，也能清空。

副模型那边：翻译时就把骨架标好了，简单模式一次都不调用副模型。深度模式的结果按楼层文本缓存，正文没变就不再调用，换音色、多翻了几楼都不会重读。长楼分批时，一楼是几批就是几次调用，加起来的字数和一次发差不多。

点单句：楼还没生成过音频时，点一句只做它所在的那一段（一次 Fish 调用），不管哪种模式；之后「朗读本楼」只做还没做的段，做过的那段直接用。

---

## 七、出问题了怎么办

**翻译没反应 / 译文是空的**

1. 「正文处理」→「检查当前楼层」，看它有没有认出正文。认不出就是提取标签填错了。
2. 「模型连接」→「测试连接」，看模型通不通。
3. 控制中心 → 「运行记录」，最新几条会写清楚哪一步失败了。

**朗读提示「酒馆的 CORS 代理没有打开」**

`config.yaml` 里 `enableCorsProxy` 改成 `true`，重启酒馆。见第六节。

**朗读提示「TauriTavern 里没有酒馆那条 CORS 代理」**

你在 TT 酒馆里，它没有 `config.yaml` 这一步。按第六节「用 TauriTavern 的看这里」架一个转发，把地址填到「接口地址」。

**朗读提示「余额不足」**

API 余额是 0。要么把模型换成 `s2.1-pro-free`，要么去 fish.audio/app/developers 充值。

**朗读提示「API Key 无效」**

Key 复制漏了字符，或者 Key 被删了。重新复制一遍。

**楼层里没有播放按钮**

- 翻译台上的「朗读（有声小说）」开关开了吗？
- 这一楼有译文吗？（读原文模式下不需要译文）
- 长聊天里往上翻的楼层，按钮是滚到那儿才挂的，等一下就有。
- 底栏在、段落后面的按钮没有：是这一段在渲染后的楼层里没定位到。现在定位只比字，不管标点和标签，基本都能找到；还是找不到的，「运行记录」里 `tts.anchors` 会列出是哪几段，把那条日志发给作者。「朗读本楼」照常能读。

**有按钮但没声音**

- 手机检查一下静音键。
- 「声音参数」里的音频格式换成 mp3。
- 看「运行记录」，Fish 返回了什么。

**副模型分析失败**

朗读会退回到翻译时的标注继续读，不会卡住。看「运行记录」里 `tts.analysis-failed` 那条。

**把日志发给作者**

「运行记录」→「导出日志」，选最近几楼或全部，会下载一个 txt。里面有发给副 API 的请求和返回，**分享前自己看一眼有没有不想给别人看的内容**。Key 不会出现在日志里。

---

## 八、手机上

- 悬浮球在手机上还是贴在屏幕右边的小条，点它打开悬浮窗。
- 悬浮窗打开先是一个小窗（楼号、状态、播放条、「读这一楼」「展开」），点「展开」是卡片，再点右上角是满屏；缩小过一次就记住。卡片是屏幕宽的 92%、高不超过七成，能拖。页签在窗底，主按钮 46px 高，播放键 56px 在右下角，右手拇指够得着。
- 详细页里改字的时候，键盘弹起来窗口会跟着缩到键盘上面，正在改的框滚到中间。
- 酒馆的输入框一获得焦点，悬浮窗自动收回贴边条，不挡键盘；点贴边条回来。
- 左撇子：「外观」页勾「左手模式」，主按钮镜像到左边。
- 每个自然段后面都有「播放」和「重新生成」，手机上一样有，一段一对，够大。每句一个的小按钮默认不加（「正文处理」页「楼层里的朗读按钮」选「每段一个，再加每句一个」就有）；改某一句发给 Fish 的内容仍然去悬浮窗的列表。
- 控制中心在手机上是顶部一排页签（翻译台、翻译规则、模型连接、正文处理、朗读、运行记录），往下滑看内容。

---

## 九、隐私和费用

- 你的 API Key 只发给你自己的酒馆和对应的服务商（翻译模型、Fish），不发给别处。
- 翻译和朗读的深度分析会把角色卡、世界书、前几楼对话发给副模型，都可以在设置里关掉。
- 翻译按翻译模型的计费规则算钱，朗读按 Fish 的字符计费规则算钱。镜译这边做的是尽量不重复调用：同样的内容只请求一次，结果存在你浏览器里。
- 音频存在浏览器的本地数据库里，换浏览器、换设备不会跟过去，清浏览器数据会一起清掉。
