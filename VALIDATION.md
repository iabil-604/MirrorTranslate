# 验证边界

## v0.29.4 · 手动分析后自动重做音频

- **自动回归**：249 项通过（refine 那一项改了期望：以前断言「旧音频被丢掉、下次播放再做」，现在断言「按新标注重做了 1 段」）。
- **无头实机**：整楼先生成 5 段（5 次 Fish 请求），让预览的分析接口换一套情绪，再「重新分析」——remade=3、只发了 3 次请求，7 句随后全部有音频、0 次新请求。
- **顺带**：预览服务器的 /api/backends/chat-completions/generate 以前不看请求内容、一律回翻译，分析改走流式以后会被这个假答案骗到；现在按 task 分流，并且加了 /preview/analysis-round 让第二次分析给出不同答案。
- **尚未验证**：重做过程中用户按停止的时序；一层楼几十段时重做的并发压力（用的是和生成同一条并发设置）。

## v0.29.3 · 弹框样式

- **无头实机**：只开悬浮窗、不碰控制中心，点「缓存」后弹框的 shadow root 里有 85206 字样式，遮罩 rgba(0,0,0,0.4) fixed 铺满，卡片浅色底、桌面 420px / 手机 358px 居中，✕ 26px，两个按钮各 40px 高。
- **原因**：`runtime.panelCss` 从未被赋值，三个弹框都读它；现在 `loadPanelCss` 读回来后存一份，弹框改成 async 等它。
- **尚未验证**：酒馆里 style.css 被主题覆盖时的表现（弹框在自己的 shadow root 里，理论上不受影响）。

## v0.29.2 · 超时重试、复用可见、用量可查

- **自动回归**：249 项通过（新增 2 项：连续两次 5xx 后第三次成功、401 只问一次；以及超时后确实发起了第二次请求——这一项真的等满了一个 10 秒超时窗口，所以这个文件跑一次要 11 秒）。
- **改动要点**：`streamFishTimestamps` 自己拿着重试循环，循环在 `withTtsTimeout` 外面，每次尝试一个新窗口；`announceTtsUnits` 在开读前列出全部段落并标出已有音频，朗读和后台生成共用；`runtime.tts.fish` 按楼层计数，`reportTtsFish` 在读完或停下时写一行。
- **尚未验证**：真实网络下的超时重试（本地是用「永不回应的 fetch + 10 秒窗口」模拟的）；重试期间用户按停止的时序；Fish 限流（429）在重试关掉时的表现没有改动。

## v0.29.0 · 三者解耦、音色屏蔽、缓存两步确认

- **自动回归**：247 项通过（新增 3 项：屏蔽的角色不进 items 也不发给 Fish、没播放过也能保存并按段命名、分析失败后楼层状态被清掉；重写 17 项：整楼一次请求、按段带编号的输入、深度分析不吃翻译骨架、边到边读的流式分析）。
- **无头实机**：Chrome headless 跑预览站点，确认——屏蔽一个角色后整楼 7 句变 5 句、楼层栏的句数跟着变、悬浮窗里那两句标「已屏蔽」且只剩「详细」；缓存弹框有 ✕ 和两个选项，选完弹二次确认，取消后消失；设置页出现「没有专属音色的角色」下拉（两个选项）、每行有屏蔽开关、勾选后立刻存盘且徽章变「🔇 不朗读」、下拉改动立刻存盘。
- **实测调用次数**（无头预览，在门口数的）：翻译+不分析 2 次（都是翻译分批，0 次分析）、翻译+简单分析 2 次（0 次额外分析）、翻译+深度分析 3 次（翻译 2 批 + 深度 1 次），并且深度那一次的结果被存下来，之后再读这一楼不再请求。预览里那一楼被翻译拆成了 2 批，所以「翻译」本身是 2 次而不是 1 次。
- **静态核对**：分析的全部入口只剩「用户动作」和「正文闭合」两类（createTtsTransport / reanalyzeTtsFloor / 设置页预览 / 设置页生成按钮 / ttsFloorClosed），楼层重绘不再触发任何请求。
- **尚未验证**：真实 Fish 与真实副模型下的端到端计时（本地只有假服务）；边到边读在非流式连接上的表现（非流式时退回整包，行为与旧版一致）；MESSAGE_EDITED 触发链在旧版酒馆上的事件名差异。

## v0.28.2 · 标签名照 Fish 官方 App

- **自动回归**：246 项通过（改了所有停顿标签的期望：S2 输出 \`[pause]\` / \`[long pause]\`，S1 输出 \`(break)\` / \`(long-break)\`，改字覆盖里用户自己写的原样发）。声音表加了 moaning（呻吟）、groaning 改为闷哼、三种群笑；中文别名表对应更新；详细页标签按钮改插官方英文。
- **依据**：用户发来的 Fish 官方 App 截图——「效果」一栏全选后插入的是 \`[laughing][chuckling][moaning][clear throat][sobbing][crying loudly][sighing][panting][groaning][crowd laughing][background laughter][audience laughing][pause][long pause]\`，与按钮顺序（大笑 轻笑 呻吟 清嗓 抽泣 大哭 叹气 喘气 闷哼 人群笑声 背景笑声 观众笑声 停顿 长停顿）一一对应。
- **尚未验证**：S2 模型对旧写法 \`[break]\` 是否也认（现在已不发）；「语气」一栏（强调 耳语 轻柔 气声 兴奋）的英文名没在截图里，仍按文档用 emphasis / whispering / soft tone；气声对应 breathy 只是猜的，没进任何自动路径。

## v0.28.1 · 深度分析：句内转折、词后声音、重读

- **自动回归**：246 项通过（新增 1 项，改 3 项）。新增：一句「前半句高兴、后半句失落」的深度回复——三处转折里不在句中的那处被丢掉，其余两处以官方情绪词落在分句开头，词后的轻笑、停顿、重读、句尾叹气各落各位，S2 模型得到「[happy] … [chuckling] ， [sad] 可是… [break] ， [lonely] 屋里就 [emphasis] 空了。 [sighing]」，S1 模型同样顺序、括号写法、没有重读；详细页「句内变化」一行写着转折处。深度提示词里有 shifts、stress、after、「前半句一种情绪、后半句另一种」「留在中间的由你按情境定」，没有 intensity 和程度词。
- **本地预览**：未跑无头 Chrome（改动在深度提示词和精简编译两处纯逻辑里，单元测试覆盖）。
- **尚未验证**：真实副模型标转折的位置是否准（at 要逐字照抄分句开头的词，抄错一个字那处就丢）；「闷哼」对应 Fish 的英文标签名不明，没加；一句里标签多了之后 Fish 的实际读法（每句上限：转折三、停顿三、词后声音两、重读两）。

## v0.28.0 · 深度分析开放

- **自动回归**：245 项通过（新增 2 项，改 6 项旧测试）。新增：深度请求——提示词里有 why、intensity、tone、pauses、shift、sounds、skeleton、previous、styles，说「感情浓度」「硬性要求」「省力原则」，不再有 direction；请求带 Fish 官方的 tones 和 sounds 表、用户手动定的 speaker、骨架；回复里的 why 留在 voice 上、详细页显示为「依据」，句内转折按官方情绪词落在那个词前面，reason 不进 Fish 文本。运行时：深度模式下翻译过的楼只发一次深度请求（骨架来自翻译标注）、走 `deepChannelId` 那条连接、请求里带角色卡和前几楼、骨架的情绪让位给深度的、详细页 depth 为 deep、再看不请求、「改这一句的分析」后仍是 deep。改动：旧版「整楼」「深度」设置回到深度（不再退回简单）；翻译请求在深度模式下也不要配音指令；深度回复里 stress / restraint / 自由指令不再进 Fish 文本，只留官方词。
- **本地预览**（无头 Chrome）：设置页「分析模式」里深度分析可选；「05 深度分析」栏摘要显示连接，副 API 下拉不再灰；切到深度分析后读一楼，分析深度为 deep。
- **尚未验证**：真实副模型对 why 字段的配合度和对省力原则的遵守（回 id 就沿用骨架）；深度请求的 token 用量（比简单分析长一截：多了角色卡、世界书、前几楼和每句的依据）；模型给的 intensity 落到没有官方强弱表的情绪词上时只剩裸词，浓度信息只在 happy / sad / angry / scared / excited 这几个词上真正到达 Fish。

## v0.27.0 · 模式矩阵、深度分析独立成模块、段落按钮只读一段

- **自动回归**：243 项通过（新增 4 项，改 5 项旧测试）。新增：翻译过的楼在不分析模式下按翻译标注读（说话人来源「翻译时的标注」、情绪进 Fish 文本、0 次请求、建传输也不问）；不分析模式下没翻译的楼按「按播放时」设置办——选「分析一次」发一次请求、结果只给这一楼、模式不变，选「直接读」0 次请求、程序认人，以前分析过的楼静默沿用；段落按钮只读一段（假 Audio 元素：两段楼层点第一段一次播放、点第二段一次、「朗读本楼」两次）；深度分析模块（`DEEP_STATUS.available` 为假、提示词在模块里、`deepRequestSettings` 只认 `deepChannelId`）。改动：简单分析跟随翻译连接、深度分析才走自己的（旧 `channelId` 迁到 `deepChannelId`）；`batchSize` 默认 0（两条分批测试显式给 12）；翻译请求在不分析模式下也带标注段但不带深度指令。
- **本地预览**（无头 Chrome）：设置页——模式选「简单分析」时「按播放时」那一项隐藏，选「不分析」时显示，说明文字随之切换；「分析每批句数」和旧的「分析用的副 API」不在页上；「05 深度分析」栏存在，摘要「迭代中，暂不可选」，里面的副 API 下拉是灰的（选项：跟随翻译的模型设置 / 各条连接），「深度分析时附带」的三个勾也在栏里。正文里五段的楼点第一段的「播放」：一次播放、底栏回到空闲，没有往下读。
- **没找到原因的一条**：不分析模式下自动翻译偶尔不触发。代码里翻译的触发只走酒馆的消息事件（生成结束 / 划动 / 编辑），朗读那边在同一事件上只安排画按钮和可选的预生成，不阻塞也不改翻译的状态；这版把不分析模式下翻译不带标注段的分叉去掉了，但那和触发无关。下次出现时导出悬浮窗日志页。
- **尚未验证**：真实酒馆里「05 深度分析」栏的折叠样式；旧设置里选过「分析用的副 API」的用户升级后深度栏里是否正确显示那条连接（逻辑上是迁移的，没实机看）。

## v0.26.3 · 整楼保存按朗读顺序剪拼

- **自动回归**：239 项通过（新增 1 项）。新增：假解码器（每次 Fish 请求的音频用请求序号做样本值）驱动的整楼拼接——一段三句，第三句改过字、第二句单独重做，拼出来的 wav 读回来是「段、第二句自己的、第三句你的」三段，每段一句长；改回自动后是「段、第二句自己的、段」；整段重做后不需要剪拼（`downloadNeedsSplice` 为假）。相邻同一条音频的句子合成一段剪，中间不丢。
- **本地预览**：未跑无头 Chrome；真实浏览器里 `decodeAudioData` 对 mp3 / Ogg 的解码和单句保存（v0.25.1）走的是同一条路。
- **尚未验证**：不同请求采样率不一致的情况（同一套参数下 Fish 不会变，变了会报中文提示让退回整段）；很长的楼一次解码的内存占用。

## v0.26.2 · 句中引号词并回旁白、单句重新生成只做那一句、改过的句子在整段里生效

- **自动回归**：238 项通过（新增 4 项）。新增：句中引号词的判定（`isEmbeddedQuote`：短、无句末标点、前面紧挨着字而非标点或说话动词、后面不是日语「と」）和折叠（`foldEmbeddedQuotes`，跳过段不合并），朗读切句和说话人着色共用——测试用的正是反馈截图那一行，切出三句：对白、含两个引号词的整句旁白、对白，锚点首尾相接；「他说“好”」「她说：“好。”」「彼は「はい」と言った」「she said "hi"」仍是对白，「所谓“朋友”」「いわゆる「友達」という」「the so-called "friend"」并回旁白；单句重新生成只发那一句、成为它自己的一条、下次查找先命中它、整段那条仍服务其他句、再点一次不拿缓存、整段重新生成把它一起丢掉；假 Audio 元素驱动的整段播放——有自己音频的句子（改过字的、单独重做的）到它时断开单独放、放完接回整段，三段三次播放，没有自己音频时整段一次放完。
- **本地预览**：未跑无头 Chrome（改动都在切句、单位和播放窗口这三处纯逻辑里，全部由单元测试覆盖）。
- **尚未验证**：中文以外的引号词判定只有基础规则；正文没空行的楼一段就是一楼，「点某一句只做它所在的那一段」在这种楼上仍然是整楼一次请求（只有重新生成改成了单句）。

## v0.26.1 · 简单分析自己认说话人、本地识别只管不分析、不分析模式可点一下分析

- **自动回归**：234 项通过（改 6 项旧测试，新增 2 项）。新增 / 改动：简单分析请求里不再带本地认出的说话人，只带手动指定的；副模型回的说话人照单全收（来源标 副模型），手动指定压在它上面并随请求发出、重新分析后仍保留；重新分析可以再改说话人，`current` 里 `manual: true` 的句子不改；不分析模式下 `reanalyzeTtsFloor` 做一次简单分析并只对这一楼生效（模式不变、同一聊天别的楼照旧不分析、再看不再请求）；调音台四档（≤15、≤35、≥65、≥85，中间沉默）带数字要求，提示词里写明是硬性要求；引擎新规则：角色写的楼里被叫到名字的「你」不算说话人（角色 +45）、你的名字旁只有动作只算 25、只剩「她」的角色靠「对面是你」认出、独自一人的楼句句归他、引号旁「老板说道」这类外人留空、翻译标注压过 80 分以下的弱证据但压不过「某某说道」「某某：」。
- **本地预览**（无头 Chrome，预览楼层是带翻译骨架的）：不分析模式下朗读页链接写「分析这一楼」，列表按本地识别 + 翻译标注显示樱井 / 玩家 / 樱井；点一下 → 列表切成副模型的说话人和情绪（happy / nervous / tender），底部药丸「✓ 简单分析」，链接变回「重新分析」，设置里的模式仍是「不分析」；详细页写「简单分析 · 说话人：副模型」，下拉框第一项「自动判断」。修掉一处：翻译骨架存在时这一楼曾被当作「已分析」弹出重新分析小窗，现在不分析模式只认副模型的分析结果。
- **尚未验证**：真实副模型对四档调音台规则的服从程度（数字要求比以前的一句话更硬，但仍靠模型自觉）；引擎新规则在多 NPC 群戏里的表现（第三人以上时退回窗口内的轮流判断）；英文 / 日文正文里的被叫者模式只有基础词表。

## v0.26.0 · 说话人本地识别、音色随音色库、缓存按完整身份

- **自动回归**：232 项通过（新增 11 项，改 3 项旧测试）。新增：说话人引擎（引号前后的人名 + 说话动词、引号旁的动作、剧本式「名字：」、上一段末尾的引出句、话里叫到的名字算被叫者、两人轮流、第三人出现停止轮流、代词 + 场上只有一人、无名字楼层全部留空）；翻译标注只在本地认不出时兜底、手动指定最高、别名归一；`pinSpeakers` 只在无人认领时保留副模型的说话人并标来源；简单分析请求带 `speaker`、提示词只问表演；音频身份（换音色 / 情绪 / 强度 / 说话人 / 语言 / 改字任一不同都不复用，旧记录按文字 + 音色匹配）；Provider Adapter 注册与 Fish 适配器；音色库条目改 ID 后角色行、旁白、对白默认、多语言表一起跟随（`followVoiceLibrary`）；验收 4（不分析模式 0 次请求、四句四种来源、译文和原文两边都认对）、验收 5（简单分析被告知说话人，副模型改名被无视、情绪 / 强度 / 语速 / 停顿被采纳，`[furious] 你 [break] 来了？`）、验收 6 / 7 / 9（整楼读完 → 改音色库 ID → 同步后 A 音色的音频不给 B 用 → 单句重新生成走 Fish 一次、副模型 0 次；直接改角色行同样）、验收 8（详细页手动改说话人 → 新音色重做、0 次分析、交回程序判断后恢复本地结果）。
- **本地预览**（无头 Chrome）：读过一楼后，句子列表四句对白按本地识别标出樱井 / 玩家 / 樱井；详细页标题旁出现下拉框（自动 / 樱井 / 玩家），灰字写「说话人：本地识别（引号前「樱井回过头，」）」；选「玩家」→ 弹「这一句改成玩家说的了…没有重新分析」、副模型 0 次、Fish 1 次且音色换成对白默认；选回「自动」→ 回到本地识别的樱井、仍 0 次副模型。把音色库条目的 ID 从 voice-girl-a 改成 voice-moved-b 后，樱井那一行自动变成 voice-moved-b，「重新生成」向 Fish 发的 reference_id 里是新 ID，副模型 0 次。手机视口（390×844，窗口展开）里详细页标题行：说话人、下拉框（145×24）、关闭键同一行，不溢出。
- **顺手修的老问题**：整楼朗读开始时预取逻辑把当前段的后半截当作独立单元再要一次 Fish（多一次请求），现在预取只做后面的段。
- **尚未验证**：真实正文里说话动词表和被叫者模式的覆盖面（中文按小说常见写法，英文 / 日文只有基础词表）；豆包等其它服务商的适配器（只搭了边界，没有实现）。

## v0.25.1 · 单句音频单独保存

- **自动回归**：221 项通过。新增：wav 编码器（RIFF/WAVE/fmt/data 四个块、单声道和双声道的声道数、采样率、16 位、data 长度和文件总长、±1 不会越界翻号、空输入给一个 44 字节的空文件）。
- **本地预览**（无头 Chrome）：读过一楼之后，详细页的「保存这一句」真的触发了一次下载，文件名「镜译-第0楼-樱井-第4句.wav」，blob: 协议；同一句从运行时再存一次拿到 63KB 和 `cut: true`（说明是从整段里剪的，不是整段）；范围外的句子号得到「这一句不在当前的朗读范围里。」；设置里「「保存到本地」保存什么」四个选项齐了。
- **尚未验证**：Safari 对 opus 的解码（解不出会报中文提示，让用户退回整段保存）；剪出来的音频在各家播放器里的边界听感。

## v0.25.0 · 带意见的定向重新分析

- **自动回归**：220 项通过。新增：只把范围内的那一句发出去、上一次的标注作为 `current` 随行、意见进 `feedback`、提示词说明是「按用户的意见修正上一次的标注」；只有被点名的那一句改了，没被提到的那一句原样不动；改完再读整楼零次请求（结果已缓存）；说话人改了之后音色跟着换、且没有发生第二次分析；按旧标注做出来的那条音频被丢掉；范围外的句子号被拒绝而不是悄悄改整楼。
- **本地预览**（无头 Chrome）：详细页里「改这一句的分析」弹出小窗，标题「重新分析第 0 楼」，范围两项默认「只改这一句」，五个常见说法点一下就填进输入框，三个按钮 refine / fresh / cancel；点「按意见改」后小窗关闭、面板抬头从「翻译骨架」变成「简单分析」（说明修正结果真的落地了）；取消不留残余；从朗读页的「重新分析」进来只给整楼一个范围。运行记录没有错误。
- **尚未验证**：真实副模型对「只回编号表示不变」这条的遵守程度（省力原则在深度分析里跑过，措辞是照搬的）。

## v0.24.1 · 正文里每段一对按钮

- **自动回归**：219 项通过（接口那条测试里多了三条下载断言）。新增：段落计划（一段一条、段内句子按朗读顺序、空输入和 null 都回空表、只读对白时段里只剩对白的那几句、同样的输入两次结果相同）；三档模式（line / sentence / off 的解析，旧的 auto 读成 line、on 读成 sentence，存下来的旧值在归一化时就迁好，不认识的值回默认）。
- **本地预览**（无头 Chrome 驱动模拟宿主）：五段的楼层挂出 5 对按钮，每对都是「播放 + 重新生成」，落在各段正文的最后几个字之后，`mes` 里没有按钮的痕迹；默认模式下每句的小按钮一个都没有，底栏还在。点第三段的「播放」：开始播、这一段的按钮亮着、整楼只亮这一个，播到第 4 句时按钮仍亮、仍在播（说明是从这一段读下去，不是读一句就停）。点「重新生成」：多一次 Fish 请求，然后接着播。设置里三个选项文案正确，选「每段一个，再加每句一个」后同一楼变成 5 对段落按钮 + 7 个 ▶ + 7 个情绪按钮，DOM 顺序是「句 ▶、句情绪、段播放、段重新生成」；选「不显示」后按钮清零、底栏还在；选回「每段一个」恢复 5 对。运行记录没有错误。
- **手机视口**（390×844，pointer: coarse）：5 对按钮照样在，「播放」66×40、「重新生成」94×40、同一行；点一下开始播、按钮亮起。双语朗读（译文 + 原文）下共 10 对，两边各 5 对，按 side 分开。
- **对抗性复查**（五个维度找问题，每条由三个怀疑者分别证伪，六条挺过来，全部已修）：重新生成放回旧音频（对象 URL 按 key 缓存）；重新生成按文字匹配会误删同文另一段的音频；停止时按钮可能卡在「生成中」（请求落地时 transport 已经没了，标记找不到人清）；段落按钮暂停后再点是重头来而不是接着读；单句播放把忙碌标记打在默认那一侧而不是要读的那一侧；诊断日志里 placed 数的是段、readable 数的是句。
- **Fish 连接设置**：超时、重试、同时生成几段三项移到「Fish Audio」栏（无头预览确认三项都在该栏、都不在「声音参数」折叠里，重试默认 1、改成 3 保存得住）；重试的单元测试：502 连续两次后第三次成功、一共三次请求，402 只请求一次并把「余额不足」抛出来。停止后楼层里没有任何按钮还亮着；段落按钮暂停再点是从暂停处接着读。
- **给别的插件用的朗读接口**（`window.__JINGYI__`，没写进更新说明，等 tokimemo 那边确认用法再公开）：单元测试——文字按行切成段、空文字和两万字以上被拒、粘进来的 HTML 先洗掉、按角色名找到登记的音色（找不到用对白默认，没给说话人用旁白音色）、一段一次 Fish 请求、说第二次零请求且 `cached` 为真、`analyze: false` 零次副模型调用而 `analyze: true` 恰好一次、朗读关着或没填 Key 时抛出能直接显示给用户的中文；门本身冻结、`status()` 和 `voices()` 的形状。无头 Chrome 里按第三方插件的用法走一遍：`window.__JINGYI__` 装上了，`speak({play:true})` 在 0.6 秒后 resolve 且已经在响、`onProgress` 一路报到 2/2、`pause` / `resume` / `stop` 都听话、`done` 正常结束、同一段文字第二次 0 次 Fish 请求、空文字得到「没有可朗读的文字。」。`play: false` 那条路：两段生成完再 resolve、`blob()` 得到一个 99KB 的 wav、`download()` 真的触发了一次下载（文件名「镜译-朗读-樱井-…wav」，blob: 协议），给名字不带扩展名会补上，带了就保留；播放过的 session 也能事后 `blob()`。生成而不播放的调用不再打断正文正在读的那一楼。
- **尚未验证**：真机手机的实际观感；各家预设美化下按钮的落点（定位靠的是句子锚点，不是 DOM 块，理论上和以前一样）；接口还没有真实调用方用过。

## v0.23.0 · 三种模式、标点情绪标签、悬浮窗分层、句子行按钮

- **自动回归**：213 项通过。新增 / 改动：模式归一化（off 合法、存过的 deep 回 simple、`deepUnlocked` 才保留 deep、`askAnalysis` 三档）；标点标签（原位替换、句首、S1 括号写法、括号里的标点不动、已有同名标签不重复、空白 / 未知 / 重复的标点丢弃、只配了标签的调音台也算有内容）；精简编译（一个官方情绪词、不加程度词、只带原文点名的语气、句首句尾各最多一个官方声音、不发中文指令、句首标签和语气不重复）；语速倾向只在副模型没说快慢时生效、不拆分请求时也生效；指纹含模式和调音台，换模式或改调音台后旧录音不再命中；翻译标注在简单模式不要 direction、声音词给官方列表，深度（开口子）要 direction，不分析模式整节不发；简单请求带 tones 和官方 sounds，提示词不含 direction。原有的深度测试打开口子后照过。
- **本地预览**（无头 Chrome 驱动模拟宿主）：悬浮窗打开是小窗（楼号、状态、按钮写「展开」，页签和三页都不显示），点展开变卡片并记住；朗读页每句行尾是「播放 / 重新生成 / 详细」；「播放」从第 4 句读起、药丸写「简单分析」；「重新生成」先进「准备中」再播；「详细」页输入「压低声音」点插入，发给 Fish 的框里出现 `[压低声音]`，「重新生成并播放」亮起；设置页分析模式是下拉框（不分析 / 简单分析 / 深度分析灰掉），旁边是「没分析过的楼，按播放时」；默认调音台展开「标点情绪标签」点「填入推荐搭配」得三行，保存后设置里 `console.marks` 三条、折叠标题写「……→[停顿]，！！→[加大音量]，？！→[惊讶]」，「……嗯。我保证。」发给 Fish 变成 `[tender][sighing] [break] 嗯。我保证。`；加一楼没翻译的正文按播放，弹出「第 1 楼还没做过简单分析」，选「直接读」后药丸写「不分析」、发给 Fish 的是 `今日は [break] 疲れたな`，再按一次不再问。
- **尚未验证**：真实 Fish 对官方标签和标点替换后的效果（体验报告的结论是官方标签比自由文本稳）；手机键盘弹起时的实际观感（只按 visualViewport 缩窗口、把输入框滚到中间）；深度模式灰掉后老用户的反应。

## v0.22.1 · 看楼不请求、分批句数可调、日志红点能消

- **自动回归**：210 项通过。新增：被动准备（`prepareTtsSegments` 带 `passive`）一次副模型都不请求、没骨架时深度为 `pending`、改句面板走同一条路也不请求；真正读的时候请求一次、任务是 `direct_voices_for_audiobook`；分析落地后再看面板直接是深度版本、不再请求。分批改成按「每批句数」：0 整楼一次、只多一点点的楼不切、40 句按 12 切成 4 批均衡落在段落边界、每批带前三句 lead、并发数不再决定批数。`batchSize` 归一化（默认 12、0 保留、4–60 钳制、非数字回默认）。
- **本地预览**（无头 Chrome 驱动模拟宿主，深度模式）：悬浮窗切到朗读页，列表 7 句、上方写「这一楼还没细读，列表里是翻译时的骨架……不会自己开始」，改句面板抬头写「翻译骨架（还没细读）」；列表开着的时候重翻这一楼，运行记录里没有任何细读条目；按播放后才出现「副模型细读了」一条，提示消失、药丸「✓ 细读这一楼」、面板抬头变「深度」。用被拒的 Fish Key 触发错误：日志红点亮起，打开日志页立即清零，日志页开着时再出错不亮，离开后再出错亮「1」。设置页有「分析每批句数」（默认 12）和手机按钮那行说明。
- **尚未验证**：真机手机上的实际观感；流式写回时列表的刷新频率（现在只做本地切句，应当无感）。

## v0.22.0 · 简单 / 深度两种模式、中文配音指令、调音台、HTML 清洗

- **自动回归**：207 项通过。新增：清洗器（标签、属性、实体、`<br>` 与段落标签变换行、script / style / 注释整块删、无标记文本原样）；设置迁移（floor → deep、stream / sentence → simple、analysis 命名的深度优先、light 提示词变 simple 提示词、调音台钳制、角色行调音台中间即 null）；方括号里的中文指令编译（指令在前、句首声音、重读 / 停顿 / 长停顿 / 转折 / 词后声音落在词上、句尾声音、S1 退回英文固定标签、`stripCues` 能全部剥掉）；翻译标注带 direction 及其指向的词进入朗读并按句校验、另一种语言只带指令和句首句尾声音；调音台转句子（中间不说话、两头各一句、规则原样）；翻译请求的标注一节要 direction、声音词表、调音台规则；简单请求的 task 与 styles；深度请求的 skeleton、previous、styles；单元一律按段、点单句只做那一段、预生成只补缺的段；分批出声与 36 句楼的测试改成段单位后照过。
- **本地预览**（无头 Chrome 驱动模拟宿主）：朗读页只剩「简单 / 深度」两个单选，原来的「谁说的、什么情绪」「流式朗读按」下拉不在了，「发给 Fish 前去掉 HTML」默认勾选，提示词框占位符换成新的简单 / 深度提示词；默认调音台 7 个滑块加规则框，拖动时数字跟着变，保存后 `tts.console.breath = 85`、规则写入；新加的角色行有折叠的调音台，拖到 20 后保存为该行的 `console.pause = 20`；老设置里的 `mode: 'floor'` 打开后显示为深度；朗读面板标签按钮变成「停顿 / 长停顿 / 重读 / 耳语 / 轻声 / 喊…」，情绪下拉插中文；模拟副模型改成回中文指令后，深度模式下发给 Fish 的文本形如「[怕被拒绝的试探，压着期待，语速放慢，句尾轻轻抬起][吸气] 明天 [停顿] ，也能在这里 [重读] 见面吗？」；简单模式下同一句由翻译骨架直接编译，是「[怕被拒绝的试探，压着期待，语速放慢] 明天，也能在这里 [重读] 见面吗？」，步骤清单写「简单分析 · 用翻译时的标注」，改句面板抬头写「翻译骨架」，一次副模型都没请求；两种模式下点句子的改句面板都能打开、各句编号对得上。
- **TauriTavern**：只有单元测试（`__TAURITAVERN__` / `__TAURITAVERN_MAIN_READY__` 识别、Fish 地址不再加 `/proxy/`、不带酒馆头、网络失败的提示写 TauriTavern 和转发地址）。无头预览里伪造 `window.__TAURITAVERN__` 之后：「经酒馆 CORS 代理发送」变灰、帮助文案换成 TT 的说法；接口地址留官方地址点「测试连接」报 TT 文案；把接口地址改成预览自己的转发地址后「测试连接」显示余额、「朗读本楼」照常生成播放。真机上的 TT 酒馆没有跑过；手册里的 Cloudflare Worker 脚本按 Workers 的 fetch 语义写的，没有实际部署过。
- **尚未验证**：真实 Fish S2 对中文方括号指令的实际效果（按 Fish 文档 S2 支持自然语言情绪指令，中文未实测）；翻译模型对新增字段的遵守程度（骨架缺字段时只是少标，不会出错）；深度润色在真实副 API 上的耗时。

## v0.21.0 · 悬浮窗重做、分批出声

- **自动回归**：201 项通过，新增 7 项：楼层行状态（已译 / 缺失 / 翻译中 / 未译）、楼层一行状态词、剩余时间估算（已完成批次均值、并行道数、超平均两倍算慢）、日志筛选与描述、未译楼层扫描；手改一段译文写回且标注保留、空译文与不存在的段被拒、只重翻一段时请求只带那一段而其余译文不动；36 句楼三路并发下 `createTtsTransport` 在第一批回来后就返回（12 句、`chunk:1`、其余在途），第一批可单独生成为 `chunk:1` 录音，全部回来后 36 句三批，第三批的句子此时还没有音频，短楼照旧整楼准备好再返回。
- **本地预览**（无头 Chrome 驱动 `scripts/preview-server.mjs`，桌面 1200×900 与手机 390×844 触屏模拟）：桌面卡片 360 宽挂在悬浮球旁，拖动后位置写入 localStorage 并在下次打开沿用；翻译页 5 行对照、标签、「重翻 / 自动 开」、模型与方案下拉；日志页本楼 / 全部筛选、点一条展开细节、关闭回列表、页签出错数；朗读页 7 句列表带说话人与情绪药丸、播放后标题变为说话人加句子、时钟 0:00 / 0:10、药丸「✓ 细读这一楼 / ✓ 生成 1/1 / 主通道」、♪ 跟着当前句、翻译页底部出现播放条并可切回、暂停后点进度条 50% 仍是暂停、「改这句」打开面板并在插入标签后才出现「重新生成并播放」、关闭面板回到列表、点第 6 句从那句读起、无控制台错误。手机：首次满屏 390×844、页签在底、楼层无每句按钮只有底栏、⤡ 缩回卡片（359×591，挂在贴边条旁）、按钮 46px、页签 47px、行 52px、播放键 56px 在右下、进度条 10px、朗读页列表在上播放器在下且列表内滚动、酒馆输入框聚焦时窗收回。
- **分批出声**（预览，36 句楼、主通道并发 2、第二批分析人为延迟 3 秒）：点播放后 0.5 秒状态已是「◌ 细读这一楼 · 0 秒 | ◌ 生成 0/1 · 18 句」，第一批的 Fish 请求先于第二批分析回来发出；模拟 Fish 流式 9 秒后开始播第 1/36 句；读第一批时第二批已预取（运行记录「第 2 批音频第 1 段已生成」），第 18 句到第 19 句之间没有出现「等待」，一直是播放态。
- **尚未验证**：真机上的拖动手感与键盘避让；分批出声在真实副 API 与真实 Fish 上的衔接（每批各自缓存，批间靠预取接上）；左手模式只核对了样式规则。

## v0.20.0 · 翻译顺手配好音、流式零请求、点单句不等整楼

- **自动回归**：194 项通过，新增 7 项：逐句标注按台词开头几个字对位（顺序打乱也能对上）、没有 head 时按顺序、数量对不上时整段一份、单句台词的段级标注和句级标注合并、Fish 词进 voice、palette 词进 label；tone 是独立标签且压过音量推出的语气，Fish 不认的 tone 丢弃，面板显示「语气」；hints 带 Fish 词和 tone；`fish.concurrency` 归一化（默认 2，上限 4）；翻译回复里的 tone 和 quotes 解析（不是标注的项丢弃）；朗读开着时翻译请求带 Fish 词表、五种语气、quotes 示例，style_repair 不带，只开着色时仍是十二个词；流式 + 翻译标注 → 零次副模型请求、一行两个说话人分开、发给 Fish 的文本为 `[very surprised][in a hurry tone] 你来了？`、面板 depth 为 annotations、没标注的楼仍问一次；全篇模式点单句只做本段（`line:2`），之后整楼仍是一次请求；三段并发 3 路同时在途、首段最后返回仍按段序入库。改了 3 项旧断言：深度回复里的 frustrated 现在在 label 里折成 angry（voice 仍是 frustrated）、S1 的 angry 强度 2 仍是 `(angry)(shouting)`；「分析失败按翻译标注读」改用深度模式验证（轻量已经不请求）；「读原文」用没有标注的楼验证请求形状。
- **本地预览**（`npm run preview`，模拟宿主 + 模拟 Fish + 模拟副模型）：翻译当前回复后楼层元数据里有三条标注（樱井 happy / 玩家 fear / 樱井 tender）；打开朗读、填假 Key、切流式，点「朗读本楼」：`ChatCompletionService.processRequest` 计数为 0，底栏「正在朗读 · 第 2/7 句」直到读完，步骤清单写「分析谁在说话 — 用翻译时的标注，3 句」「生成第 1 段 1 句 · 2.6 秒」，面板当前句写「翻译标注 · Fish 默认音色 · 已有音频」、情绪「温柔（中）」；「读什么」的四档文案、模式说明、「声音参数」里的「同时生成几段」（默认 2）都在。
- **尚未验证**：真实翻译模型对 `quotes` / `head` / `tone` 的遵守程度（对不上时退回整段一份，不会比以前差）；真实 Fish 对并发请求的限流阈值（429 时错误提示会指向「同时生成几段」）；全篇模式点单句只做本段、并发生成只有自动回归覆盖，没在预览里点过。

## v0.19.1 · 细读分批并行、指定副 API、进度条拖动

- **自动回归**：187 项通过，新增 4 项：批次按段落边界切、少于 30 句或单路时不切、每批带前三句 lead；36 句楼在三路并发下发 3 批（同时在途 3 个）、第二三批各带前一批末尾三句、合并后 36 句都有说话人与情绪、再次准备零请求；朗读指定通道 c2 后请求走 c2 的模型与地址，通道不存在或未选时原样跟随翻译；lead 进入深度与轻量两套请求且第一批不带。
- **本地预览**（`npm run preview`，模拟宿主 + 模拟 Fish + 模拟副模型）：朗读页「读什么」出现「分析用的副 API」下拉，选项为「跟随翻译的模型设置（主通道）/ 主通道 · labeler / 快模型 · flash」，选「快模型」后设置里 `tts.channelId = c2`、`ttsRequestSettings` 解析到 c2，选回空则跟随；「副模型提示词（高级）」两套各有「填入内置的再改 / 复制内置提示词 / 恢复默认」，填入后文本框等于内置提示词、复制得到的文本等于内置轻量提示词、恢复后文本框与设置都为空；朗读面板「复制这一楼的分析」得到 7 句 JSON（说话人、情绪、配音指令、发给 Fish 的文本如 `[happy] 你来了啊`）。把楼层换成 36 句对白后「朗读本楼」：主通道并发 2 → 细读分 2 批各 18 句、同时在途 2 个、第二批 lead 为 16–18 句、运行记录 `tts.analysis` 两条带批次、`tts.analysis-batches` 记「2 批、2 路并行」，步骤清单显示「细读这一楼 36 句 ✓ / 生成整楼音频 36 句」。
- **进度条拖动**（预览，合成指针事件 + 真实点击各验一遍）：播放中点 60% 处跳到 0:06 / 0:10、进度 61%；从 20% 拖到 40% 跟手；暂停后点 50% 进度到 50% 仍是「已暂停」，点到 100% 停在 99.5% 不自动接下一句；键盘 ← 退 5%；继续后从该处播完、自动进入第二段；播完（待播放、0%）后点 30% 从第 6 句重新读；真实点击进度条后滑块获得焦点并跳转。滑块 8px 高、`cursor: grab`、末端圆点在有内容时常显；手机（hover: none）下 10px 高、圆点 18px。
- **尚未验证**：真实 Fish 与真实副 API 上的并行批次（速率限制时的表现）；触屏上拖动的手感；深度分析在两个批次之间人物情绪衔接的实际质量。

## v0.19.0 · 细读省力、进度、双语、定位

- 自动回归 183 项通过。新增覆盖：分析缓存键只随文本 / 深度 / 语言面变化（名单变了、多了一楼不重读）；hints 随请求发出、只回 id 的句子沿用底色、部分回答与底色合并；自定义提示词的占位符替换；砍掉的字段不再进 voice；原文由译文推标签（语言不带、停顿重音不带）；标点压缩只作用于发给 Fish 的文本、不作用于对齐；只按字母定位（预设改引号、丢逗号、折叠、中间插标签都能找到，按钮仍落在闭引号后）；流式按句一句一请求；双语各自生成。
- 本地预览：见下方记录。
- 尚未在真实酒馆验证：懒加载挂按钮在几百楼的长聊天里的实际观感；双语模式在真实预设下的按钮位置；副模型对 hints 省力原则的遵守程度。

## v0.18.3 · 角色表范围与朗读页折叠

- 自动回归 178 项通过，新增：按聊天保存的角色表（借用角色卡的表 → 本聊天自己的表 → 清空后不回退 → 切回按角色卡）、`voiceScope` 归一化。
- 本地预览：朗读页四栏折叠、折叠标题的状态摘要、角色行折叠成一行并展开编辑、新加行默认展开、「删掉没绑音色的行」与「清空角色表」；见本节下方截图记录。

## v0.18.1 – v0.18.2 · 手机端、切页动画、多国语言入口

- **手机端**（预览 375×812 模拟）：楼层每句后的播放 / 情绪按钮与底栏正常换行，`mes` 不含朗读数据；悬浮窗翻译页与朗读页均在视口内（朗读页高 600 以内、宽 344，超出时窗体内滚动）；朗读设置页在 355px 内无横向溢出，角色行按「名字 / 别名 → 锁定 → 音色 + 音色库 → 工具 → 语言音色」堆叠，音色库行按「名字 / 语言 → ID → 移除」堆叠。触控设备上播放 / 情绪按钮放大到 1.55em，朗读面板按钮最小 40px。
- **悬浮窗切页动画**：切页时 `.jy-mini` 加 `is-resizing` 并以行内高度从旧页高度过渡到新页高度（transitionend 或 420ms 兜底清除），新页带 `is-entering` 从所在一侧滑入淡入，页签下划线按 `data-mini-tab` 滑动；系统「减少动态效果」时全部跳过。预览窗格在后台时浏览器不推进动画帧，只验证了类名、行内高度与 CSS 过渡声明（`getAnimations` 列出高度过渡与页面入场动画），动画本身的观感未在前台核对。
- 177 项自动回归通过；打包后的副本单独验证通过。

## v0.18.0 · 朗读第二版

本地预览（模拟酒馆 + 模拟 Fish + 模拟副模型）和自动回归两处验证；没有安装到真实酒馆，也没有对真实 Fish 跑深度分析编译出来的标签。

- **自动回归**：177 项通过。新增覆盖：自定义对白 / 跳过符号切句与嵌套、按文字判断语言、深度分析请求与回复解析（词不在句中的停顿 / 重音 / 转折被丢弃）、Voice → Fish 标签编译（前置标签上限四个、句内停顿 / 强调 / 转折插入、句尾非语言声、S1 只用固定集合）、语速音量分档与按档拆请求、无音色时不带 `reference_id`、有无音色混排拒绝、按语言 / 锁定解析音色、录音覆盖表与跨单位查找（新的优先、改过的不冒充原句）、缓存键随改写变化、WAV 拼接、设置迁移（sentence → stream、model → auto）与音色库归一化；端到端：深度分析带角色卡与前几楼、按语速拆成两次请求、流式按段生成且换到全篇后不重复请求、整楼录音覆盖流式的每一句、无音色照常生成、改写单句生成 `sentence:N` 录音并被优先使用、恢复后不再冒充、后台逐段预生成且第二次为零、世界书识别（副模型 / 标题兜底）。
- **本地预览**（`npm run preview`）：翻译台开关打开后朗读页出现；朗读页填 Key、模型、旁白 / 对白默认音色，添加音色库条目并保存；「从世界书识别角色」导入樱井（别名桜井）与老胡两行，均为「跟随默认音色」；从音色库为樱井选音色后变为「🔒 专属音色」，加一行日语音色后保存，设置里 `voices.ja` 正确；翻译后楼层挂出 7 个播放按钮 + 7 个情绪按钮，底栏有暂停 / 停止 / 朗读面板，`mes` 不含朗读数据；「朗读本楼」触发深度分析（7 句，3 句带指令）并按语速拆成两段整楼音频，逐句高亮推进；暂停后底栏变「继续」、面板变 ▶；下一段跳到第 2 段第 2 句，上一段回到第 1 段；点情绪按钮打开悬浮窗朗读页，显示樱井那句的场景 / 情绪 / 心理 / 意图 / 人物状态 / 说话习惯、发给 Fish 的文本 `[happy] 你来了啊`；在光标处插入「叹气」并把语速改成 0.75 后「用这个重新生成」生成 `sentence:4` 录音并立即播放，「恢复自动」后回到自动版本；切到流式朗读后点第 6 句直接从整楼录音里播放，运行记录没有新的 `tts.recording`。
- **尚未验证**：真实 Fish 对深度分析编译出的自由文本标签（`[holding back]`、`[hesitant]`、`[hoarse]`）的实际效果；真实酒馆里世界书条目事件的时机；手机真机上悬浮窗朗读页的滚动；`drama-3-preview` 模型；切页动画在前台的实际观感。

## v0.17.1 · 朗读开关位置

预览中确认：翻译台「自动接续翻译」下方出现「朗读（有声小说）」一条，默认关闭、「朗读设置 →」隐藏、左侧无「朗读」页签，正文处理页不再有朗读勾选框；打开开关后页签出现、朗读页顶部同名开关同步勾选、「朗读设置 →」出现且点击后切到朗读页。166 项自动回归通过。

## v0.17.0 · 朗读

真实接口、宿主源码、本地预览三处分别验证；没有安装到真实酒馆里跑。

- **Fish 真实接口**（2026-09-14，作者提供的 Key 与公开音色，只在本地脚本中使用，未写入仓库）：
  - `/v1/tts/stream/with-timestamp` 为 `text/event-stream`，每帧 `event: message` + `data`，字段为 `audio_base64`、`content`、`alignment{segments,audio_duration}`、`chunk_seq`、`chunk_audio_offset_sec`。中文按单字对齐、去掉标点；`[happy]` 不出现在 segments 里，在 `content` 中被替换为空格；同一 chunk 的快照逐次变长，以最新一份为准。
  - 多说话人：`reference_id` 传数组 + `<|speaker:N|>` 在 `s2.1-pro-free` 上返回 200；换人处被切成新的 text chunk，第二段带独立 offset。
  - `/v1/tts` 直接返回 `audio/mpeg`；`/wallet/self/api-credit` 返回余额；无效 Key 返回 401 `Invalid token`；该账户 API 余额为 0，`s2-pro` 返回 402。
  - CORS：对 `api.fish.audio` 的 OPTIONS 预检返回 404 `no_route`，所有响应均无 `Access-Control-Allow-Origin`，浏览器无法直连。
  - 用扩展自己的适配层（`buildFishPayload` → `createSseParser` → `buildGlobalTimeline` → `alignSpansToTimeline`）对真实接口跑一楼旁白 + 对白 + 数字：5 句全部对齐，覆盖率 100%，总时长 12.63 秒，与 mp3 码率估算的 12.70 秒一致。
  - 测试用例 `test/tts.test.mjs` 中的对齐数据取自上述真实返回。
  - 日文原文（2026-09-15，同一 Key、`s2.1-pro-free`）：segments 按词切并保留汉字假名原文（`夕暮れ`、`教室`、`振り返っ`、`笑っ`），数字 123 拆成 `1`、`2`、`3`；6 句全部对齐、覆盖率 100%，总时长 12.35 秒。
- **SillyTavern `/proxy/` 源码审计**（release 分支 `src/middleware/corsProxy.js`、`src/server-main.js`、`src/util.js`）：代理需 `enableCorsProxy`；开启时对 `/proxy/` 跳过 CSRF，未开启时返回 404 `CORS proxy is disabled…`（CSRF 在其前面，因此请求需带 `X-CSRF-Token` 才能拿到这句提示）；`bodyParser.json` 在代理之前，请求体按 JSON 重新序列化转发；响应以流 pipe 回来，但不转发响应头，401 改写为 400。扩展据此自行补音频 MIME、按响应体里的 status 判断 401。Fish 在浏览器声明支持 zstd 时仍返回 gzip。
- **本地预览**（`npm run preview`，预览服务器模拟 `/proxy/https://api.fish.audio/…`，按说话人生成音调 WAV 与逐字时间戳）：翻译后楼层挂出 7 个按钮，对白按钮落在 `<q>` 之外，`mes` 不含任何朗读数据；整篇发送 1 次请求、7 句覆盖率 100%，再次调用命中 IndexedDB；点单句从句首前 0.08 秒播到窗口终点自动暂停；「朗读本楼」高亮随句推进、停止后清除；单句模式首次点击只请求 `/v1/tts` 一次，再点无请求；刷新页面后缓存仍在；测试连接、查询音色名、分析预览、导入与保存音色表均可用；可爱风、极简风、原文折叠三款美化下按钮均位于译文区、底栏样式随美化切换；华美吸血鬼与极简毛玻璃主题下「朗读」页配色正确。
- **一般模式与朗读语言**（预览）：默认关闭时控制中心只有 5 个页签、楼层无按钮；在正文处理页打开后朗读页签出现、朗读页上的同名开关同步勾选、楼层挂出 7 个按钮；切到原文后按钮移到日文原文行、底栏显示「读原文」、楼层 ID 带 `|source`、整篇音频 7 句覆盖率 100%；在朗读页关掉时页面只剩标题与开关、楼层按钮立即撤下，离开后页签隐藏。
- **自动回归**：166 项通过，新增 31 项，覆盖切句、分析请求与解析、标准结构、音色解析、情绪标签、Fish 请求体与拆段、SSE 断帧、快照覆盖、真实数据对齐、数字重同步、播放窗口、缓存键、错误提示、代理请求头、渲染后定位、来源标签回退、设置归一化、缓存淘汰与降级、Key 脱敏，以及楼层读取、分析缓存与失败回退、单句与整篇请求的端到端流程。
- **手机悬浮入口**（预览 375×812 手机模拟）：修复前贴边形态为 34×42、圆角 `999px 0 0 999px`，进度环实测 26×26 贴在核心区顶部，圆心比图标高 4px；修复后为 38×34、圆角 `10px 0 0 10px`、圆环隐藏，图标与条的纵向中线同为 455，点击检测确认条覆盖到 x=372（视口宽 375）；模拟翻译中 60% 时圆点变为 `#f0c56e`、底部细线 `scaleX(0.6)`；派发 resize 后切到桌面宽度，形态变回圆环、位置回到原先的 x=333。预览窗格模拟视口时不会触发 resize 事件，这一步是手动派发的。
- **尚未验证**：真实酒馆开启 `enableCorsProxy` 后的完整链路；超过「每次最多字数」的长楼层在真实 Fish 上的分段表现；iOS Safari 播放 opus；付费模型 s2-pro 的实际效果；真机上的悬浮贴边条。

## v0.12.0 · 本次改动

按作者要求，本轮只进行相关本地检查与模拟页面查看，未连接真实翻译 API，未安装到真实酒馆，也未进行多版本运行测试。

- 语法检查通过；核心与上下文的相关检查通过，正文方案的 8 项检查通过。
- 覆盖空前后缀、自然同名符号保留、主模型过滤、旧标记迁移、重复换样式、已有滑动页译文复用及保存失败回退。
- 覆盖原生正则完整字段、顺序与开关随方案往返，导入后重新分配 ID，隔离镜译规则组，保留其他原生规则。
- 模拟页面确认默认方案、紧凑管理行、底部折叠美化入口及方案切换。独立阅读预览使用同一份美化正则和样式，支持明暗切换与原文展开。
- 移除了清单最低版本限制；旧版脚本加载提供幂等启动回退。源码接口比对不等同于旧版酒馆实际运行通过。

原生正则接口依据 SillyTavern [1.18.0 的引擎](https://github.com/SillyTavern/SillyTavern/blob/8172dcd0ee672d3cd9a5e5f7af134f91a45cd2b8/public/scripts/extensions/regex/engine.js)、[管理器](https://github.com/SillyTavern/SillyTavern/blob/8172dcd0ee672d3cd9a5e5f7af134f91a45cd2b8/public/scripts/extensions/regex/index.js)及 [1.14.0 上下文](https://github.com/SillyTavern/SillyTavern/blob/9c9be90821ffd6132b40b5f04982522a61d7ad30/public/scripts/st-context.js)核对。使用 `extensionSettings.regex`、`saveSettingsDebounced()` 与原生显示正则；未依赖私有的正则列表刷新函数。路由资料为 2026-08-18 快照的 A0、D5、E5 与扩展/API 参考技能，未采用设计目录候选。

## 既有版本验证记录

以下是此前版本的证据与真实宿主验证参考，不代表本轮新增运行结果：

1. `validation/sillytavern-1.18.0.snapshot.json` 来自 SillyTavern `1.18.0` 标签源码审计，证明所调用的上下文符号和事件存在；
2. `npm run validate` 执行本地语法、核心行为、清单和能力契约检查。

这些检查不能替代真实宿主验收。后续有需要时可在目标 SillyTavern 中参考以下检查项：

- 从 Git URL 安装并刷新后扩展正常激活；
- 魔法棒入口与扩展设置入口均能打开控制中心；
- 控制中心不会在启动时自动遮挡聊天；
- 主页面、提示词页、设置页、日志页均可切换和保存；
- “跟随当前连接”和“独立副 API”各完成一次测试翻译；
- 新回复、切换已有滑动页、编辑、切换聊天期间的写回行为正确；
- 下一次主模型请求中不包含带不可见标记的中文镜像；
- 控制中心内的更新按钮能在真实 Git 安装中完成检查、安装和刷新；
- 禁用、启用、更新和清理扩展后没有重复事件或残留面板。

## 本轮本地验收

- 路由回执：`consult-tavernweave-library`、`sillytavern-extension-dev`、`code-quality-workflow`；资料快照 `2026-08-18`；采用 A0、D5、E5 的扩展契约、控制中心、表单和移动端边界；代码质量复核只返回 ST-A0；目录候选仍为 proposed，未采用；
- 目标：交付 0.8.0，在保留简体中文完整规则包的同时加入每方案目标语言、来源语言逐段自动识别、统一重试、标签检查和最新楼层安全合并；
- 红线：不依赖 TavernHelper；未复制参考插件的源代码、素材或视觉资产；提示词按已获授权的长规范与分层检查写法为正文翻译重新编写；程序负责的提取、排除、包裹、标记、写回和日志机制不进入模型提示词；模型的 `<thinking>` / `<think>` 内容不写进聊天正文；不在真实宿主验收前声称完全兼容；
- 数据迁移：设置结构升级为 schema 8；旧 `bodyTag` 自动迁为 `bodyTags`，默认值仍只有 `story_scene`；未修改过的 v0.6/v0.7 默认提示词升级为新版，用户自定义提示词继续保留；旧方案自动取得默认目标语言“简体中文”；
- 提取边界：每个已配置正文标签只选最后一组完整内容；不同标签共同参与同一次首次请求；标签输入接受名称或完整尖括号形式；排除块支持嵌套、自闭合和原位保留；相互交叉而无法安全还原的标签会明确报错；
- 分段边界：完整空行才创建新段落；段内换行、首尾空白和原文顺序均保留；每个空行段落只生成一个目标语言镜像块；
- 调用边界：正常首次翻译把全部选中正文一次发送，不按标签或段落拆批；返回不完整或命中绝对禁用表达时仍沿用恢复与定向修正流程，不伪造缺失译文；
- 并发写回：开始请求时记录的语义签名只包含实际待译段落；写回前重新读取最新楼层。排除标签内容或正文标签外内容变化时在最新结构上合并，真正待译正文变化时按统一预算重译；不使用固定等待或持续抢写；
- 自动回归：35 项通过，覆盖设置/提示词迁移、目标语言变量、非中文通用规则、多标签末组选择、标签检查、空行分段、段内换行、嵌套与自闭合排除、语义签名、整段前后缀、部分写回恢复、可见 think/thinking 解析、提示词职责边界、世界书上下文、参数排除、模型列表和生命周期契约；
- 本地浏览器结果：v0.8.0 控制中心可打开；主页面正确识别 2 段；提示词页 10 个标准条目默认全部收起且一次只展开一个，目标语言切到 English 后显示通用规则提示；设置页“检查当前楼层”报告 `<story_scene>` 1 组、采用第 1 组、2 个可译段落；副 API 请求中途模拟插入 `<image_prompt data-preview="late-insert"/>` 后标签原位保留、2 段译文完整写回，并产生 `translation.rebase` 日志；桌面对话框 client/scroll 宽度均为 761px；390×844 下对话框、内容和文本域无横向溢出；浏览器控制台无警告或错误；
- 通用扩展清单脚本会把带 `?v=0.8.0` 的 manifest 资源误判为非 `.js` / `.css` 文件；项目自身检查会去掉查询参数后核对真实文件，并保留查询参数解决更新后的混合缓存问题；
- 尚未关闭：真实 SillyTavern 中从 GitHub 更新到 0.8.0 后的入口恢复、真实副 API、真实世界书激活、滑动页/聊天切换保存流程，以及与实际文生图扩展同时写入同一楼层的验收。

## v0.8.1 本地验收

- 目标：修复独立副 API 拉取模型后的首次选择体验，并让空回、截断、格式漂移问题能够取得每次请求的完整返回证据；本轮没有修改翻译提示词、请求拆分、解析、重试或写回策略；
- 模型列表：拉取成功后不再把排序后的第一个模型写入当前预设；原生下拉框显示接口返回的全部模型，并保持“请选择”状态，用户明确选择后才同步到手动模型输入框；接口无法列出模型时仍可手动填写；
- 诊断记录：每次成功收到副 API 返回后，在解析前保存完整原始返回；对象响应保留 `content`、`reasoning`、`reasoning_content`、`thinking` 及其他原始字段，字符串响应原样保留；完整返回默认收起并可单条复制；
- 隐私边界：安全摘要不包含完整返回；完整日志明确提示可能含正文和模型思考，常见凭据字段与 Bearer/查询参数密钥形态会隐藏；达到本地存储预算时整条淘汰最旧记录，不截断仍保留的单条响应；
- 自动回归：36 项通过，新增完整返回保存、安全摘要隔离、凭据脱敏、模型列表不自动选择以及完整日志入口的覆盖；语法检查和项目清单检查通过；
- 本地浏览器结果：v0.8.1 模拟宿主中拉取到 `translator-pro`、`translator-small` 后，两项同时可见且没有自动选中；选择 `translator-small` 后手动输入框同步；测试请求生成 `translation.raw-response` 日志，展开后可见完整 `content` 与 `reasoning`；
- 已知验证边界：上述为模拟宿主和模拟 OpenAI 兼容端点的本地证据，尚未证明真实 SillyTavern 安装更新、真实副 API 的思考字段形态或浏览器存储配额表现。跟随当前连接时，扩展只能记录 SillyTavern `generateRaw` 实际返回给扩展的字符串；宿主未暴露的隐藏思考无法由扩展补取。

## v0.8.2 本地验收

- 目标：把平行线、控制标签、装饰线和段内换行的机械处理从副模型提示词移回脚本，同时保持正常首次翻译只有一次整批请求；
- 数据迁移：设置结构升级为 schema 9，旧设置原样保留并补入空白的“原样保留白名单”；未修改翻译提示词及用户自定义方案；
- 分段与写回：完整空行仍决定外层段落；段内语义行使用连续内部 ID，一次发送给副 API，返回后按原行序组合进同一个多行镜像块；新镜像块使用成对不可见边界，生成拦截、重新翻译和部分恢复均能完整识别；旧版单行不可见标记继续支持剥离与单行恢复；
- 结构保护：排除标签整块原样保留；其他转义或未转义标签视作透明结构，标签语法不进入翻译单元；纯符号、纯边框和仅含标签的行由内置规则原样保留；
- 自定义白名单：支持整行、`prefix:` 行首和 `/正则/flags` 三种规则；命中行不建立翻译 ID、不发送给副 API且保持原位置；错误正则和空 prefix 会给出行号；暂不支持任意行内子串白名单，行内整块排除继续使用排除标签；
- 检查报告：显示空行段落数、实际翻译行数、自定义白名单命中、内置结构保留行及透明结构标签；
- 自动回归：38 项通过，新增多行镜像剥离/恢复、旧单行标记兼容、白名单三类规则、无效规则、透明容器标签、转义排除标签以及容器闭合位置覆盖；语法、清单一致性和 diff whitespace 检查通过；
- 尚未关闭：真实 SillyTavern 1.18.0 中的更新重载、真实长正文副 API、平行线预设、移动端设置页，以及与实际文生图扩展同时写入同一楼层的联动验收。

## v0.9.0 交付

- 重写通用规范和清单，采用全篇简短核对，取消逐 ID 检查报告；保留单批请求和现有解析协议。
- 旧默认文本按完整内容匹配升级，用户修改不自动覆盖；提供带方案备份的手动采用入口。
- 界面改为紧凑深色浅紫风格，提示词列表默认收起；新增模型搜索和首页当前方案入口。
- 按本轮要求只做语法及现有核心检查；真实模型翻译质量、桌面/手机界面和交互由人工验收。

## v0.10.0 前端重建

- 重写静态界面与整个面板样式，改用五个工作区；提示词条目编辑器移动到独立区域并保留编辑位置。
- 对照上一版本静态界面的 action、设置字段和连接字段，三类控制均无遗漏。
- 本地模拟宿主快速查看了桌面工作台及窄屏规则编辑，修正编辑聚焦引起的外层窗口滚动；正式酒馆和人工操作体验待验收。
- 提示词文本、翻译调用、提取/写回算法没有修改。公开版本 0.10.0。

- 追加五主题、方案导入导出、更新入口与移动端留边调整后，按用户明确要求停止浏览器操作，仅做 JavaScript 语法与文件差异检查；追加部分的外观、导入导出和更新交互由用户验收。
